# 2026-02-12 릴리즈 노트 - 접근성 업데이트 (이슈 #88)

## 추가

- Windows 통합 설치기 확장
  - `integration/installer/install-integration.ps1`
    - `Auto` 모드: `sparse-package` 우선 시도 + 실패 시 `legacy-shell` fallback
  - `integration/installer/uninstall-integration.ps1`
    - sparse package 제거 시도 + legacy 정리
  - `integration/installer/detect-integration.ps1`
    - `mode`, `sparsePackage`, `legacyShell` 상태 보고
- Win11 1차 우클릭 경로용 sparse package 스크립트 추가
  - `integration/package/install-sparse-package.ps1`
  - `integration/package/uninstall-sparse-package.ps1`
  - `integration/package/README.md`
- 앱 설정 메뉴에 `Windows 통합 진단/복구` 버튼 추가
- 통합 패키지/셸 확장 구성 동기화
  - `integration/package/AppxManifest.xml`
  - `integration/shell/BAEFRAME.ContextMenu/*`

## 지원 안내

- Windows 11 1차 우클릭 지원(통합 설치 필요)
- 지원 확장자: `.mp4`, `.mov`, `.avi`, `.mkv`, `.webm`

## 비고

- 환경/정책에 따라 sparse package 등록이 실패할 수 있으며, 이 경우 설치기는 자동으로 legacy 모드로 폴백한다.
- 최종 모드는 `detect-integration.ps1` 또는 앱의 진단/복구 흐름에서 확인할 수 있다.
