# 이어붙이기 자동 재생 정지 수정 계획서 — 작성 기록

- **계획서**: `C:\BAEframe\BAEFRAME_수정계획_이어붙이기-자동재생-정지.md` (저장소 밖 — 실행 커밋에 섞이지 않게 하는 관례)
- **작성일**: 2026-08-21 ~ 2026-08-24
- **피드백 출처**: 사용자 채팅 보고(구글독스 아님) — "이어붙이기 자동 재생 중 세 번째 연속 재생쯤 멈추는 경우가 있고, 그러다 재생되기도 함"
- **기준 스냅샷**: main `fdc7116`(= v2.1.1-beta 배포 빌드)
- **작업 구성**: 6개 — ① 진단 계측(렌더러 WARN+ 파일 기록 등) ② persistence IPC 데드라인(3초)+게이트 직렬화 ③ 자동 전환 게이트 실패 정책 교정(PR #175 회귀 취소) ④ 체인 예외 격리+전환 20초 감시자+file:exists 3초 데드라인 ⑤ 멈춘 세션 스페이스 1회 재개 ⑥ mpv 전진 판정 실측화

## 검증 이력 요약

1. **로그 분석**: 실사용 로그 2일치 전수 파싱 — v2.1.1 세션에서 정지 2회를 로그로 확정(연속 전환 3~4회차, 다음 항목 `file:get-info` 이전에서 침묵 사망), v2.0.3 세션은 같은 재생목록 11회 연속 무사고 → 회귀 판정.
2. **다관점 조사**: 7개 관점(persistence 게이트·오버레이 호스트·연속 체인·mpv eof·회귀 diff·협업 지연·단위 하네스) 병렬 조사 → 발견 30건을 발견마다 1~2명의 적대적 반박자로 교차검증 + 완결성 비평(미검토 경로 3건 추가 식별). 하네스 실측 2건: 전환 겹침 시 IPC 7배·게이트 9.9초, 호스트 무응답 시 게이트 영구 pending(타임아웃 전무).
3. **2패스 계획서 검증**: 패스 1(사실) — before/앵커 39개 대조(38개 편차 0), worktree 사본에 전량 적용 후 테스트 실행(25종 GREEN, 신규 단언 RED→GREEN 실측). 패스 2(시뮬레이션) — 블로커 4건(하네스 불일치·sendTool 래핑 누락·catch 세션 오폭·스페이스 이중 토글) + 개선 12건.
4. **델타 재검증**: 발견 전부 반영 후 개정판 전량 재적용 — 치환 47건 실패 0, 8개 스위트 GREEN(271/301/340/155/19/60/82/5), RED 왕복으로 신규·갱신 단언 8건 전건 판정 확인, 금지어·정규식 특별 확인 5종 통과. **문제 0건 — 델타 검증 통과.**

## 참고

- 테스트 기준선(수정 전): 25종 1,925 pass / 0 fail, lint 0 error. `fabric`·`electron`·`jsdom`은 devDependencies — 착수 전 `npm install` 필수.
- 이 파일은 계획서 본문의 사본이 아니다(진실은 계획서 하나). 실행 에이전트는 구현 후 `DEVLOG/이어붙이기-자동재생-정지-구현.md`를 별도로 작성한다.
- 후속 체인: 실행 에이전트 구현 → PR 생성 → `codex-review-loop` → `baeframe-release-deploy`.
