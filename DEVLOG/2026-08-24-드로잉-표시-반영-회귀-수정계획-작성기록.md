# 드로잉 표시·반영 회귀 수정계획 작성기록 (2026-08-24)

> 이 문서는 **작성 과정의 기록**이다. 실행 사양은 계획서 원본이 유일한 진실이다 — 본문을 여기 복사하지 않는다.

## 개요

| 항목 | 내용 |
|---|---|
| 계획서 | `C:\BAEframe\BAEFRAME_수정계획_드로잉-표시-반영-회귀.md` |
| 범위 | 사용자 보고 3증상 — A(B키 진입 시 툴바 미표시), B-1(이탈 시 획 소실·재진입 미표시), B-2(타임라인 드로잉 레이어 미생성) + 부수 결함 1건 |
| base | `main` HEAD `fdc7116` (= v2.1.1-beta 배포본, 08-21 17:33 배포와 소스 일치 확인) |
| 작업 | 6개 — PR ① `fix/드로잉-표시-반영-회귀`(작업 1~5) / PR ② `feat/드로잉-타임라인-레이어-투영`(작업 6) |
| 테스트 기준선 | 25개 스위트 1,925 pass / 0 fail |
| 상태 | **계획서 완성·검증 완료 — 실행 대기** |

## 원인 확정 요약

- **저장 사슬은 정상** — 재현 세션(08-24 19:47, `m008,009_3.bframe`) 실측: 스트로크 9개 전부 자동저장 성공, `drawingsV3` revision 11 실재. "저장 안 됨"은 표시 계층 오인.
- **증상 A**: 툴바 opacity 100ms 페이드(1b7500a) × 투명 오버레이 창의 프레임 미생산 × 과거 수정 fdc9195(invalidate 1회)의 원천 불완전성 × PR #175(22eedc0)가 가림막이던 hide→show 부수 재합성 제거(환경 회귀). 사용자의 "이전에 고쳤는데 반영 안 됐나?"에 대한 답: 반영은 살아 있으나 원래 불완전했던 수정이 노출된 것.
- **증상 B-1**: `renderActiveScene`의 "동기 clear + rAF 재도색" 구조(원설계) + passive 표시 경로 부재(원설계) — 재진입 순간 지워진 캔버스가 present됨.
- **증상 B-2**: 회귀 아님 — 파일럿 승격(07-23) 시 타임라인 연동 미이식 + 3c91f5b의 CSS 전면 은닉. 구빌드 산출물도 legacy 키프레임 0 실측.
- 부수: `runPersistenceSourceRefresh`의 installSource 반환값 무시(5123650 도입 — PR #174 아님).

## 검증 이력 (전 과정 샌드박스 실행 기반)

1. **조사 1R**: 8관점 병렬 + 적대검증 6(전건 REFUTED) + 완결성 비평 → 레거시 경로 가설 반증, 실측 판별 실험 설계.
2. **실측 판별**: 배포 runtime-profile 정상, 재현 세션 로그·`.bframe` 산출물 대조 → "저장 정상/표시 결함" 확정.
3. **조사 2R**: 4관점 + 적대검증 4(전건 CONFIRMED) + 수정 청사진.
4. **독립 리뷰 2패스**(초판): blocker 4(작업 5 비교식·작업 4 동일성 단언·작업 6 정규식 2건) + major 6(작업 6 (e) 완전 중복, 판별력 0 테스트 3건 등) + minor 8 → 전건 반영.
5. **델타 1차**: 사실 패스 pass(1,933/0 실측·RED 전건 실측). 시뮬 패스 major 2(같은 id 수화 미탐 → force 분리 / passive 삭제 경로 → (e)② 가드) + minor 5 + note 5 → 전건 반영.
6. **델타 2차**: 1,933/0 재확인. major 1(수화 경로 `!snapshot` 오판 → `clearWhenMissing` 분리) + minor 4 + note 2 → 전건 반영.
7. **마이크로 검증**(최종 반영분): clearWhenMissing·작업 5 recovering 분기 적용·실행·RED 확인 — 결과는 계획서 리뷰 이력 참조.

누적: blocker 4 + major 9 + minor 17 + note 7을 **구현 전에** 검출·반영. 최종 실측: 전 25스위트 1,933 pass / 0 fail (기준선 1,925 + 신규 8), 판별 테스트 전건 RED→GREEN.

## 이번 라운드의 방법론 메모

- 계획서 검증에 "정적 대조"만이 아니라 **샌드박스에 계획서 코드를 실제 적용·실행**(기존 파손 + 신규 테스트 RED/GREEN 실측)을 도입 — 정규식 시뮬레이션만으로는 못 잡는 blocker 4건을 이 방식이 잡았다.
- 신규 회귀 테스트는 "수정 전 RED"를 의무화(판별력 없는 테스트 3건이 초판에 있었음). [판별]/[안전망] 표기 구분.
- 조사 단계의 적대적 검증(1R 전건 REFUTED)이 그럴듯한 오답(legacyBypass 가설 등)을 걸러냈고, 완결성 비평이 실측 판별 실험을 설계해 결정적 증거(산출물 대조)로 이어졌다.

## 후속 체인

1. 실행 에이전트가 계획서로 구현 → PR ① 생성
2. `codex-review-loop` 스킬로 PR 리뷰 루프 (명시적 완료 신호까지)
3. 머지 후 `baeframe-release-deploy`로 빌드·배포·해시 검증
4. PR ② (작업 6) 동일 체인
5. 배포 타임스탬프를 메모리에 기록 — 다음 라운드 대조 기준
