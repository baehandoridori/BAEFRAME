# 3차 빌드 전 마지막 이슈 해결 리스트업

> 작성일: 2026-01-18
> 작성자: Claude (claude-opus-4-5-20251101)
> 목적: Claude 최종점검 + 코덱스 검증 + GPTPRO 분석을 **통합 검증**하여 확정된 문제 리스트업
> 브랜치: `claude/fix-canvas-crash-zjK8M`

---

## 문서 개요

### 교차검증 프로세스

1. **1차**: Claude가 `3차-빌드전-클로드-최종점검.md` 작성 (39건 문제 제시)
2. **2차-A**: 코덱스 모델이 실제 코드와 대조하여 교차검증 (28건 확정)
3. **2차-B**: GPT-PRO가 별도 분석하여 `BAEFRAME_Issues_GPTPRO.md` 작성 (27건)
4. **3차**: Claude가 두 검증 결과를 통합 분석하여 최종 확정 (본 문서)

### 교차검증 모델 비교

| 항목 | 코덱스 메시지 | GPTPRO 문서 | 비고 |
|------|-------------|-------------|------|
| 총 이슈 | 28건 | 27건 | 유사 |
| 1단계 긴급 | 2건 | **6건** | GPTPRO가 더 엄격 |
| 2단계 심각 | 13건 | 10건 | |
| 3단계 주의 | 9건 | 5건 | |
| 4단계 경미 | 4건 | 6건 | |
| Claude 정확도 평가 | 6/10 | - | |

### GPTPRO가 추가 발견한 중요 문제 (Claude 누락)

| 문제 | 위험도 | 설명 |
|------|--------|------|
| 원격(Drive) 최신 변경 보호 실패 | 🔴 1단계 | localModified가 거의 now로 잡혀서 남이 한 수정 덮어씀 |
| reloadAndMerge가 미저장 작업 덮어씀 | 🔴 1단계 | 편집 중(isDirty) 고려 부족 |
| 메인 JSON parse/stringify 동기 실행 | 🟠 2단계 | 큰 파일에서 프리즈 |

### 최종 통합 심각도 분류

| 위기 수준 | Claude 원본 | 코덱스 | GPTPRO | 최종 확정 |
|-----------|-------------|--------|--------|-----------|
| 1단계 긴급 | 10건 | 2건 | 6건 | **4건** |
| 2단계 심각 | 25건 | 13건 | 10건 | **13건** |
| 3단계 주의 | 0건 | 9건 | 5건 | **6건** |
| 4단계 경미 | 4건 | 4건 | 6건 | **6건** |

---

## 요약 테이블

### 🔴 1단계 긴급 (4건)

| # | 문제 | 파일:라인 | 상태 |
|---|------|----------|------|
| 01 | 저장 겹침 + 비원자적 저장 → 파일 손상 | review-data-manager.js:195,685-712 | ⬜ 대기 |
| 02 | 원격(Drive) 최신 변경 보호 실패 | review-data-manager.js:261-275 | ⬜ 대기 |
| 03 | reloadAndMerge가 미저장 작업 덮어씀 | review-data-manager.js:416-436 | ⬜ 대기 |
| 04 | 앱 내부 XSS + IPC 경로 검증 부재 (체인) | app.js:5094-5141, ipc-handlers.js:64-68 | ⬜ 대기 |

### 🟠 2단계 심각 (13건)

| # | 문제 | 파일:라인 | 상태 |
|---|------|----------|------|
| 05 | JSON 손상 파일 복구 경로 없음 | ipc-handlers.js:78-92 | ⬜ 대기 |
| 06 | 앱 종료 시 렌더러 응답 없으면 종료 불가 | index.js:298-323 | ⬜ 대기 |
| 07 | Redo가 잘못된 스냅샷 복원 | drawing-manager.js:314-316 | ⬜ 대기 |
| 08 | 드로잉 이미지 캐시 무제한 | drawing-manager.js:686-720 | ⬜ 대기 |
| 09 | 메인 JSON parse/stringify 동기 실행 | ipc-handlers.js:15-74 | ⬜ 대기 |
| 10 | FFmpeg 캐시 정리 sync I/O + 에러 무시 | ffmpeg-manager.js:670-744 | ⬜ 대기 |
| 11 | 파일 watch 정리 누락 (누적) | ipc-handlers.js:117-250 | ⬜ 대기 |
| 12 | 딥링크 프로토콜 불일치 | index.js:66-99, open.html:194-200 | ⬜ 대기 |
| 13 | 크래시/무응답 복구 UX 부족 | window.js:179-202 | ⬜ 대기 |
| 14 | Slack 확인 execSync (메인 블로킹) | ipc-handlers.js:1034-1050 | ⬜ 대기 |
| 15 | save() await 누락 경로 존재 | app.js (다수) | ⬜ 대기 |
| 16 | 웹 뷰어 XSS (author) | web-viewer/app.js:1820-1827 | ⬜ 대기 |
| 17 | FFmpeg kill/timeout 스트림 정리 미흡 | ffmpeg-manager.js:496-506 | ⬜ 대기 |

### 🟡 3단계 주의 (6건)

| # | 문제 | 파일:라인 | 상태 |
|---|------|----------|------|
| 18 | 댓글 이미지 유령 첨부 | comment-manager.js:360-469 | ⬜ 대기 |
| 19 | copyFile/deleteFile IPC 미구현 | preload.js, review-data-manager.js:328-357 | ⬜ 대기 |
| 20 | validators NaN/Infinity 검증 부족 | shared/validators.js:131 | ⬜ 대기 |
| 21 | watch 오류 사용자 미통지 | ipc-handlers.js:164-174 | ⬜ 대기 |
| 22 | Timeline.destroy() 리스너 정리 없음 | timeline.js:2138-2161 | ⬜ 대기 |
| 23 | 캔버스 크기 0 처리 부족 | drawing-canvas.js:91,111 | ⬜ 대기 |

### 🟢 4단계 경미 (6건)

| # | 문제 | 파일:라인 | 상태 |
|---|------|----------|------|
| 24 | 영상 비율 0 나누기 가능 | video-player.js:530-536 | ⬜ 대기 |
| 25 | canvas null 체크 없음 | drawing-canvas.js:35 | ⬜ 대기 |
| 26 | preload 리스너 중복 가능 | preload.js:98-121 | ⬜ 대기 |
| 27 | 마이그레이션 오류 처리 일관성 부족 | shared/schema.js | ⬜ 대기 |
| 28 | logger 실패 상위 전파 없음 | logger.js:101-108 | ⬜ 대기 |
| 29 | API 패턴 불일치 (send vs invoke) | preload.js:70 | ⬜ 대기 |

---

## 🔴 1단계 긴급 상세

### [01] 저장 겹침 + 비원자적 저장 → 파일 손상

**파일**: `renderer/scripts/modules/review-data-manager.js`
**라인**: 195 (save), 685-712 (자동저장)
**검증**: ✅ 코덱스 + GPTPRO 모두 확인

#### 문제 설명

- 저장이 겹칠 수 있음 (락/원자적 저장 없음)
- 자동저장 + 수동저장 + Undo/Redo 연쇄 시 문제
- `.bframe` 깨짐 → 열기 실패 → 작업 유실

#### 권장 해결책

```javascript
// 1. 저장 큐/락 추가
async save(options = {}) {
  if (this._savePromise) {
    return this._savePromise;
  }
  this._savePromise = this._doSave(options).finally(() => {
    this._savePromise = null;
  });
  return this._savePromise;
}

// 2. 원자적 저장 (temp → rename)
async _doSave(options) {
  const tempPath = `${this.currentBframePath}.tmp`;
  await window.electronAPI.saveReview(tempPath, data);
  await window.electronAPI.renameFile(tempPath, this.currentBframePath);
}
```

---

### [02] 원격(Drive) 최신 변경 보호 실패

**파일**: `renderer/scripts/modules/review-data-manager.js`
**라인**: 261-275, 165-172
**검증**: ✅ GPTPRO 발견, Claude 누락

#### 문제 설명

- `localModified`가 거의 `now`로 잡혀서 원격 최신을 보호 못 함
- 남이 저장 후 내가 저장하면 "남이 한 수정이 사라짐"

#### 권장 해결책

```javascript
// 영역별 dirty 분리 + 원격 최신 우선 정책
async _mergeBeforeSave(localData) {
  const remoteData = await window.electronAPI.loadReview(this.currentBframePath);

  // 원격이 더 최신이면 해당 영역은 원격 우선
  if (remoteData.comments?.lastModified > localData.comments?.lastModified) {
    // 로컬 변경 중 충돌 항목만 병합
  }
}
```

---

### [03] reloadAndMerge가 미저장 작업 덮어씀

**파일**: `renderer/scripts/modules/review-data-manager.js`
**라인**: 416-436
**검증**: ✅ GPTPRO 발견, Claude 누락

#### 문제 설명

- 파일 변경 감지 시 편집 중(isDirty) 고려 부족
- 그리던 드로잉이 갑자기 사라짐

#### 권장 해결책

```javascript
async reloadAndMerge() {
  // 편집 중이면 사용자에게 확인
  if (this.isDirty) {
    const proceed = await dialog.confirm(
      '파일이 외부에서 변경되었습니다. 현재 작업을 유지하면서 병합할까요?'
    );
    if (!proceed) return;
  }
  // ...
}
```

---

### [04] 앱 내부 XSS + IPC 경로 검증 부재 (체인)

**파일**: `renderer/scripts/app.js:5094-5141`, `main/ipc-handlers.js:64-68`
**검증**: ✅ 코덱스 + GPTPRO 모두 확인

#### 문제 설명

- `innerHTML`로 사용자 입력(author) 직접 삽입 → 코드 실행 가능
- `saveReview` IPC에 경로 제한 없음
- 체인: 악성 `.bframe` 열기 → XSS → IPC로 파일 훼손

#### 권장 해결책

```javascript
// 1. escapeHtml 적용
<div class="thread-comment-author">${escapeHtml(marker.author)}</div>

// 2. IPC 경로 검증
const ALLOWED_EXTENSIONS = ['.bframe', '.json'];
function validatePath(filePath) {
  const ext = path.extname(filePath).toLowerCase();
  if (!ALLOWED_EXTENSIONS.includes(ext)) {
    throw new Error('허용되지 않은 파일 형식');
  }
  // 경로 이탈 방지
  const normalized = path.normalize(filePath);
  if (normalized.includes('..')) {
    throw new Error('잘못된 경로');
  }
  return normalized;
}
```

---

## 🟠 2단계 심각 상세 (요약)

### [05] JSON 손상 파일 복구 경로 없음
- `.bak` 자동 복구 없음, 손상 = 접근 불가

### [06] 앱 종료 시 렌더러 응답 없으면 종료 불가
- 타임아웃/폴백 없음 → 강제 종료 필요 → 저장 누락

### [07] Redo가 잘못된 스냅샷 복원
- `historyIndex+1` 참조 오류

### [08] 드로잉 이미지 캐시 무제한
- LRU/정리 부재 → 오래 쓰면 느려짐/멈춤

### [09] 메인 JSON parse/stringify 동기 실행
- 큰 파일에서 프리즈

### [10] FFmpeg 캐시 정리 sync I/O + 에러 무시
- 캐시 정리 중 프리즈/실패 묻힘

### [11] 파일 watch 정리 누락
- 여러 파일 열면 watch 누적 → 무거움

### [12] 딥링크 프로토콜 불일치
- 웹 뷰어 URL 형식 ≠ 앱 파서

### [13] 크래시/무응답 복구 UX 부족
- 사용자는 "그냥 멈춤"만 봄

### [14] Slack 확인 execSync
- 동기 시스템 호출 → 순간 멈춤

### [15] save() await 누락 경로
- fire-and-forget → 저장 겹침 위험

### [16] 웹 뷰어 XSS
- author 미이스케이프

### [17] FFmpeg 스트림 정리 미흡
- 자원 정리 모호 → 누적 시 성능 저하

---

## Claude가 과장한 문제 (수정 불필요)

| 문제 | Claude 주장 | 실제 확인 결과 |
|------|------------|---------------|
| 날짜 파싱 NaN 오류 | 🔴 심각 | ❌ 재현되지 않음 |
| 레이어 ID 충돌 | 🟠 높음 | ❌ 보정 로직 존재 |
| DrawingCanvas 리스너 누수 | 🟠 높음 | ❌ AbortController로 정리됨 |
| 레이어 UI 미갱신 | 🟠 높음 | ❌ layersChanged 발생함 |

---

## 권장 수정 우선순위

### 🔥 빌드 전 필수 (TOP 5)

1. **[01] 저장 큐 + 원자적 저장** - 데이터 유실 방지
2. **[02] 동기화 병합 설계 보강** - 원격 최신 보호
3. **[04] innerHTML 제거 + IPC 경로 제한** - 보안
4. **[05] 손상 파일 로드 복구** - .bak 탐색/안내
5. **[08] 드로잉 이미지 캐시 제한** - LRU 등

### ⚡ 빌드 직후 (우선)

- [03] reloadAndMerge 미저장 보호
- [06] 앱 종료 타임아웃
- [07] Redo 버그
- [16] 웹 뷰어 XSS

### 📋 후속 작업

- 2단계 나머지
- 3~4단계

---

## 빌드 진행 권장 여부

- [ ] 즉시 빌드 가능
- [ ] 일부 수정 후 빌드 권장
- [x] **심각한 문제 해결 후 빌드 필요** (🔴 1단계 4건 우선)

### 최소 수정 후 빌드 가능 조건

1. ✅ 저장 큐/락 추가 (동시성 문제)
2. ✅ XSS 취약점 수정 (escapeHtml)
3. ✅ IPC 경로 검증 추가

---

## 참고 문서

| 문서 | 경로 |
|------|------|
| Claude 최종점검 | `DEVLOG/3차-빌드전-클로드-최종점검.md` |
| GPTPRO 분석 | `DEVLOG/BAEFRAME_Issues_GPTPRO.md` |
| 코드 심층 분석 | `DEVLOG/2026-01-16-코드-심층-분석-보고서.md` |

---

> 작성: Claude (claude-opus-4-5-20251101)
> 교차검증: 코덱스 모델 + GPT-PRO
> 최종 확인: 2026-01-18
