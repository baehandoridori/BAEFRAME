# 사용자 리뷰 UI/UX 개선 구현 계획

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사용자 리뷰에서 수집된 3가지 UI/UX 이슈(드로잉 모드 B 토글, 타임라인 프레임 격자, 코멘트 중첩 시 길이 조절)를 단계적으로 해결한다.

**Architecture:** 각 Phase를 독립 실행 가능한 단위로 구성. 순수 로직(Phase 2 tier resolver, Phase 3 클러스터링/레인 할당)은 별도 모듈로 분리해 `node --test`로 단위 테스트. UI 와이어링은 수동 테스트 매트릭스로 검증. 데이터 모델(.bframe) 및 Liveblocks 프로토콜은 불변.

**Tech Stack:** Electron 28 / Vanilla JS (ES modules) / node --test / ESLint / Prettier

**Spec 참조:** [`DEVLOG/2026-04-17-사용자리뷰-UIUX개선-설계.md`](./2026-04-17-사용자리뷰-UIUX개선-설계.md)

---

## Scope Check

이 계획은 세 개의 약한 결합 Phase를 포함하지만, 각 Phase는 독립적으로 완료·롤백 가능하다. 공유 의존성은 없고 `renderer/scripts/modules/user-settings.js`만 Phase 2에서 공통 수정된다. 분리된 계획으로 쪼개지 않고 순차 실행 구조로 둔다.

---

## File Structure

### Phase 1 (수정만)
- `renderer/scripts/app.js:6583-6592` — 키 핸들러 수정
- `renderer/index.html:627` — UI 단축키 라벨 정정
- `CLAUDE.md:117` — 문서 정정

### Phase 2 (생성 + 수정)
- **생성**: `renderer/scripts/modules/frame-grid-tiers.js` — 순수 단계 결정 함수 (히스테리시스 포함)
- **생성**: `scripts/tests/frame-grid-tiers.test.js` — 단위 테스트
- **수정**: `renderer/scripts/modules/timeline.js:770-895` — `_updateFrameGrid`/`_renderFrameGrid` 교체
- **수정**: `renderer/scripts/modules/user-settings.js:161-186, 520+` — `showFrameGrid` 필드 및 getter/setter
- **수정**: `renderer/index.html:716-727` — 격자 토글 버튼 삽입
- **수정**: `renderer/styles/main.css` — 토글 버튼 스타일, ½초/¼초 색상
- **수정**: `renderer/scripts/app.js` — 토글 버튼 이벤트 연결
- **수정**: `package.json` — `test:frame-grid` 스크립트 추가

### Phase 3 (생성 + 수정)
- **생성**: `renderer/scripts/modules/comment-cluster.js` — 구간 클러스터링 + 레인 할당 순수 함수
- **생성**: `scripts/tests/comment-cluster.test.js` — 단위 테스트
- **수정**: `renderer/scripts/modules/timeline.js:1918-2003` — `renderCommentRanges` 확장 (클러스터 배지 + 펼침)
- **수정**: `renderer/styles/main.css:2865-2971` — 클러스터 배지 스타일, 트랙 높이 transition
- **수정**: `package.json` — `test:cluster` 스크립트 추가

### 파일 경계 설계 원칙
- 순수 로직(`frame-grid-tiers.js`, `comment-cluster.js`)은 DOM·프로젝트 의존성 없이 import만으로 테스트 가능
- UI 모듈(`timeline.js`, `app.js`)은 순수 로직을 import해서 사용만 (로직 복제 금지)
- 단위 테스트는 `scripts/tests/`에 모으고 `npm run test:*` 스크립트로 개별 실행

---

## Spec 변경 사항 1건

Spec에서 `userSettings.timeline.gridVisible`(중첩 키)로 명시했으나, 기존 `user-settings.js`의 관례가 **평탄 키**이므로(`showCommentThumbnails`, `showPlexusPanel` 등) 일관성을 위해 `showFrameGrid` 평탄 키로 구현한다. 이 계획 전체에서 `showFrameGrid`를 사용한다.

---

## Chunk 1 — Phase 1: 드로잉 모드 B 키 순수 토글

### 단계 목표
B 키 핸들러에서 `if (!state.isDrawMode)` 조건을 제거하여 항상 토글되도록 하고, 진입 시에만 브러시를 선택한다. UI 라벨 및 CLAUDE.md 단축키 표도 "D" → "B"로 정정.

### Task 1.1: 현재 동작 확인 (베이스라인)

**Files:** (읽기만)
- `renderer/scripts/app.js:6583-6592`

- [ ] **Step 1: 현재 코드 확인**

Run:
```bash
sed -n '6583,6592p' renderer/scripts/app.js
```

Expected: `if (!state.isDrawMode) { toggleDrawMode(); }` 블록이 존재 확인.

- [ ] **Step 2: dev 모드 실행으로 현재 버그 재현**

Run: `npm run dev`
수동 확인:
  - B 누름 → 드로잉 모드 진입 + 브러시 자동 선택 ✓
  - B 다시 누름 → **아무 반응 없음** (버그 재현 확인)

### Task 1.2: 키 핸들러 수정

**Files:**
- Modify: `renderer/scripts/app.js:6583-6592`

- [ ] **Step 1: 핸들러 교체**

`renderer/scripts/app.js:6583-6592`의 기존 블록:

```javascript
if (userSettings.matchShortcut('drawMode', e)) {
  e.preventDefault();
  if (!state.isDrawMode) {
    toggleDrawMode();
  }
  const brushBtn = document.querySelector('.tool-btn[data-tool="brush"]');
  if (brushBtn) brushBtn.click();
  return;
}
```

를 다음으로 교체:

```javascript
if (userSettings.matchShortcut('drawMode', e)) {
  e.preventDefault();
  const wasOff = !state.isDrawMode;
  toggleDrawMode();
  if (wasOff) {
    // 진입 시에만 브러시 자동 선택
    const brushBtn = document.querySelector('.tool-btn[data-tool="brush"]');
    if (brushBtn) brushBtn.click();
  }
  return;
}
```

- [ ] **Step 2: lint 검사**

Run: `npm run lint`
Expected: 새로 추가된 라인에 오류 없음. 기존 경고만 출력되면 OK.

- [ ] **Step 3: 수동 테스트 (dev 모드)**

Run: `npm run dev`

| 초기 상태 | 액션 | 기대 결과 |
|---|---|---|
| 드로잉 OFF | B 누름 | 드로잉 ON + 브러시 자동 선택 |
| 드로잉 ON (브러시) | B 누름 | 드로잉 OFF |
| 드로잉 ON (지우개 선택 중) | B 누름 | 드로잉 OFF (브러시 재선택 X, 지우개 상태 유지) |
| 드로잉 ON (도형 선택 중) | B 누름 | 드로잉 OFF |
| 오디오 모드 | B 누름 | 무반응 (기존 차단 로직 `state.isAudioMode` 유지 확인) |
| 입력 필드 포커스 | B 누름 | 텍스트 입력 'b' 들어감 (`matchShortcut`이 input 차단) |

모두 통과해야 다음 단계 진행.

### Task 1.3: UI 라벨 정정

**Files:**
- Modify: `renderer/index.html:627`

- [ ] **Step 1: 단축키 힌트 텍스트 변경**

`renderer/index.html:627`:

```html
<span class="shortcut-hint" id="btnDrawModeHint">D</span>
```

를 다음으로:

```html
<span class="shortcut-hint" id="btnDrawModeHint">B</span>
```

- [ ] **Step 2: 동적으로 덮어쓰는 곳 확인**

Run:
```bash
grep -rn "btnDrawModeHint" renderer/scripts/
```

- 결과에 동적 업데이트 코드가 있다면 해당 위치도 확인해야 함
- 없다면 정적 HTML만으로 충분

- [ ] **Step 3: 수동 확인**

Run: `npm run dev`
- 우측 상단 그리기 모드 버튼 툴팁/라벨에 "B"가 표시되는지 확인

### Task 1.4: CLAUDE.md 단축키 표 정정

**Files:**
- Modify: `CLAUDE.md:117`

- [ ] **Step 1: 문서 수정**

`CLAUDE.md:117`의 `| D | 그리기 모드 |`를 `| B | 그리기 모드 |`로 변경.

- [ ] **Step 2: 다른 문서에서 "D 그리기" 참조 확인**

Run:
```bash
grep -rn "D.*그리기\|D.*드로잉\|drawing.*D" docs/ DEVLOG/ 2>/dev/null | head -10
```

관련 참조가 있으면 동반 정정.

### Task 1.5: 커밋

- [ ] **Step 1: 변경 사항 검토**

Run: `git status && git diff --stat`
Expected: `app.js`, `index.html`, `CLAUDE.md` 3개 파일 수정.

- [ ] **Step 2: 커밋**

```bash
git add renderer/scripts/app.js renderer/index.html CLAUDE.md
git commit -m "$(cat <<'EOF'
fix: 드로잉 모드 B 키 순수 토글 + UI/문서 라벨 정정

기존 핸들러는 `!state.isDrawMode` 조건으로 OFF→ON만 가능했으나,
조건을 제거하고 `toggleDrawMode()`를 항상 호출하도록 수정.
브러시 자동 선택은 진입 시에만 수행.

- app.js: 키 핸들러 순수 토글로 변경
- index.html: 단축키 라벨 "D" → "B"
- CLAUDE.md: 단축키 표 정정
EOF
)"
```

### Phase 1 엣지케이스 & 회귀 방지

- [ ] **시나리오 1**: 드로잉 도중 스페이스(재생) → B
  - 기대: 재생 상태에서도 드로잉 OFF 동작
- [ ] **시나리오 2**: 드로잉 모드에서 Undo(Ctrl+Z) 후 B
  - 기대: Undo 후에도 정상 토글
- [ ] **시나리오 3**: Liveblocks 협업 상황에서 다른 사용자가 드로잉 중일 때 본인이 B
  - 기대: 본인의 드로잉 모드만 토글, 다른 사용자 영향 없음
- [ ] **시나리오 4**: 사용자 단축키 커스터마이즈(drawMode를 Ctrl+D로 변경) 후 동작
  - 기대: 변경된 단축키로도 순수 토글 동작 (`matchShortcut`이 키 해석)

### Phase 1 롤백

단일 커밋이므로:
```bash
git revert <phase1-commit-hash>
```

---

## Chunk 2 — Phase 2: 타임라인 프레임 격자 단계식 + 토글

### 단계 목표
1. 순수 함수 `resolveFrameGridTier(pxPerFrame, prevTier)` 생성 (단위 테스트 포함)
2. `_updateFrameGrid`의 기존 3단 분기를 제거하고 신규 5단 계단식 로직으로 교체
3. `.timeline-toolbar-right`에 격자 토글 버튼(`btnGridToggle`) 추가
4. `userSettings.showFrameGrid` 평탄 키로 영속화, 기본값 `true`
5. 히스테리시스(±3%) MVP 포함

### Task 2.1 (TDD): 순수 tier 결정 함수 — 테스트 작성

**Files:**
- Create: `scripts/tests/frame-grid-tiers.test.js`

- [ ] **Step 1: 테스트 파일 생성**

`scripts/tests/frame-grid-tiers.test.js` — **반드시 Phase 3 스타일의 dynamic import를 사용**해야 한다. `renderer/scripts/modules/`는 ES module이므로 `require()`로 로드할 수 없음. 다음 템플릿 그대로 생성:

```javascript
/**
 * frame-grid-tiers 단위 테스트
 * 실행: npm run test:frame-grid
 */

const { test } = require('node:test');
const assert = require('node:assert/strict');

let resolveFrameGridTier, TIER;
test('모듈 로드', async () => {
  const mod = await import('../../renderer/scripts/modules/frame-grid-tiers.js');
  resolveFrameGridTier = mod.resolveFrameGridTier;
  TIER = mod.TIER;
});

test('pxPerFrame < 0.5: 5초 강조선만', () => {
  const t = resolveFrameGridTier(0.2, null);
  assert.equal(t.showFiveSec, true);
  assert.equal(t.showOneSec, false);
  assert.equal(t.showHalf, false);
  assert.equal(t.showQuarter, false);
  assert.equal(t.showFrame, false);
  assert.equal(t.tier, TIER.FIVE_SEC);
});

test('pxPerFrame = 0.5 임계: 1초 진입', () => {
  const t = resolveFrameGridTier(0.5, null);
  assert.equal(t.showFiveSec, false);
  assert.equal(t.showOneSec, true);
  assert.equal(t.tier, TIER.ONE_SEC);
});

test('pxPerFrame = 1.0: ½초 추가', () => {
  const t = resolveFrameGridTier(1.0, null);
  assert.equal(t.showOneSec, true);
  assert.equal(t.showHalf, true);
  assert.equal(t.showQuarter, false);
  assert.equal(t.tier, TIER.HALF_SEC);
});

test('pxPerFrame = 2.0: ¼초 추가', () => {
  const t = resolveFrameGridTier(2.0, null);
  assert.equal(t.showHalf, true);
  assert.equal(t.showQuarter, true);
  assert.equal(t.showFrame, false);
  assert.equal(t.tier, TIER.QUARTER_SEC);
});

test('pxPerFrame = 4.0: 매 프레임 추가', () => {
  const t = resolveFrameGridTier(4.0, null);
  assert.equal(t.showQuarter, true);
  assert.equal(t.showFrame, true);
  assert.equal(t.tier, TIER.EVERY_FRAME);
});

test('히스테리시스: 0.485 (이탈)에서 TIER.ONE_SEC 유지 (직전이 ONE_SEC)', () => {
  const t = resolveFrameGridTier(0.49, TIER.ONE_SEC);
  assert.equal(t.tier, TIER.ONE_SEC);
  assert.equal(t.showOneSec, true);
});

test('히스테리시스: 0.48 이하에서는 FIVE_SEC로 하강', () => {
  const t = resolveFrameGridTier(0.48, TIER.ONE_SEC);
  assert.equal(t.tier, TIER.FIVE_SEC);
});

test('히스테리시스: 상승 진입은 임계 + 3% (0.515)', () => {
  const t1 = resolveFrameGridTier(0.51, TIER.FIVE_SEC);
  assert.equal(t1.tier, TIER.FIVE_SEC, '0.51은 아직 FIVE_SEC');
  const t2 = resolveFrameGridTier(0.52, TIER.FIVE_SEC);
  assert.equal(t2.tier, TIER.ONE_SEC, '0.52는 ONE_SEC로 상승');
});

test('엣지: pxPerFrame = 0 (duration 0 상황)', () => {
  const t = resolveFrameGridTier(0, null);
  assert.equal(t.tier, TIER.FIVE_SEC);
  // 렌더러 쪽에서 추가 guard 필요 — 여기선 크래시 없음만 확인
});

test('엣지: 매우 큰 pxPerFrame (100)', () => {
  const t = resolveFrameGridTier(100, null);
  assert.equal(t.tier, TIER.EVERY_FRAME);
  assert.equal(t.showFrame, true);
});

test('엣지: 음수/NaN 입력 — 안전한 기본값', () => {
  const t1 = resolveFrameGridTier(-1, null);
  assert.equal(t1.tier, TIER.FIVE_SEC, '음수는 FIVE_SEC로 수렴');
  const t2 = resolveFrameGridTier(NaN, null);
  assert.equal(t2.tier, TIER.FIVE_SEC, 'NaN도 안전 기본값');
});
```

- [ ] **Step 2: package.json에 테스트 스크립트 추가**

`package.json`의 `scripts` 섹션에 추가:

```json
"test:frame-grid": "node --test scripts/tests/frame-grid-tiers.test.js",
```

- [ ] **Step 3: 테스트 실행 — 실패 확인**

Run: `npm run test:frame-grid`
Expected: **실패** — 모듈이 아직 없음 (`Cannot find module '../../renderer/scripts/modules/frame-grid-tiers.js'`)

### Task 2.2 (TDD): 순수 tier 결정 함수 — 구현

**Files:**
- Create: `renderer/scripts/modules/frame-grid-tiers.js`

- [ ] **Step 1: 모듈 생성**

`renderer/scripts/modules/frame-grid-tiers.js` — **순수 ES module**로 작성. `module.exports`나 CommonJS 구문을 **절대 섞지 말 것**(`SyntaxError` 발생). 테스트에서는 dynamic import로 로드하므로 ESM 전용으로 충분:

```javascript
/**
 * 프레임 격자 단계 결정 — 순수 함수 (DOM 의존 없음)
 *
 * 타임라인 줌 레벨(pxPerFrame)과 직전 단계를 받아
 * 현재 표시할 격자 단계와 레이어 플래그를 반환한다.
 * 단계 경계 근처에서 깜빡임을 방지하기 위해 ±3% 히스테리시스를 적용한다.
 */

// 단계 식별자
export const TIER = Object.freeze({
  FIVE_SEC: 0,     // 5초 강조선만
  ONE_SEC: 1,      // + 1초
  HALF_SEC: 2,     // + ½초
  QUARTER_SEC: 3,  // + ¼초
  EVERY_FRAME: 4   // + 매 프레임
});

// 상승(진입) 임계값
const UP_THRESHOLDS = [
  { tier: TIER.ONE_SEC, pxPerFrame: 0.5 },
  { tier: TIER.HALF_SEC, pxPerFrame: 1.0 },
  { tier: TIER.QUARTER_SEC, pxPerFrame: 2.0 },
  { tier: TIER.EVERY_FRAME, pxPerFrame: 4.0 }
];

// 히스테리시스 비율
const HYSTERESIS_PCT = 0.03;

/**
 * 주어진 pxPerFrame에서 도달 가능한 최고 단계를 반환 (히스테리시스 없음)
 */
function baseTier(pxPerFrame) {
  if (!Number.isFinite(pxPerFrame) || pxPerFrame <= 0) {
    return TIER.FIVE_SEC;
  }
  let result = TIER.FIVE_SEC;
  for (const t of UP_THRESHOLDS) {
    if (pxPerFrame >= t.pxPerFrame) result = t.tier;
    else break;
  }
  return result;
}

/**
 * 히스테리시스 적용된 단계 반환
 * @param {number} pxPerFrame
 * @param {number|null} prevTier - 직전 프레임의 tier (없으면 null)
 * @returns {{tier: number, showFiveSec, showOneSec, showHalf, showQuarter, showFrame}}
 */
export function resolveFrameGridTier(pxPerFrame, prevTier) {
  const b = baseTier(pxPerFrame);

  let tier = b;
  if (prevTier !== null && prevTier !== undefined) {
    // 직전 단계가 있으면 히스테리시스 체크
    if (b > prevTier) {
      // 상승: 임계 × (1 + 3%) 이상이어야 상승
      const upThreshold = UP_THRESHOLDS[prevTier];
      if (upThreshold && pxPerFrame < upThreshold.pxPerFrame * (1 + HYSTERESIS_PCT)) {
        tier = prevTier;
      }
    } else if (b < prevTier) {
      // 하강: 임계 × (1 - 3%) 이하여야 하강
      const downThreshold = UP_THRESHOLDS[prevTier - 1];
      if (downThreshold && pxPerFrame > downThreshold.pxPerFrame * (1 - HYSTERESIS_PCT)) {
        tier = prevTier;
      }
    }
  }

  return {
    tier,
    showFiveSec: tier === TIER.FIVE_SEC,
    showOneSec: tier >= TIER.ONE_SEC,
    showHalf: tier >= TIER.HALF_SEC,
    showQuarter: tier >= TIER.QUARTER_SEC,
    showFrame: tier >= TIER.EVERY_FRAME
  };
}
```

**참고**: `main/recent-files-store.js`는 CommonJS(node 컨텍스트), 반면 `renderer/scripts/modules/`는 모두 ES module(브라우저 컨텍스트). 이 모듈은 후자. 테스트 파일에서는 `await import(...)`로 dynamic 로드하여 두 체계를 모두 지원.

- [ ] **Step 2: 테스트 실행 — 통과 확인**

Run: `npm run test:frame-grid`

Expected: 모든 `test(...)` 블록 PASS.
실패 시 진단:
  - `Cannot find module` → 경로 확인, Node 20+ 필요
  - 테스트가 `resolveFrameGridTier is not defined`로 실패 → Step 1 '모듈 로드' 테스트가 먼저 실행되어 참조를 바인딩하는지 확인
  - 기타 → 모듈 파일의 ESM export 구문 확인 (`export const TIER`, `export function resolveFrameGridTier`)

- [ ] **Step 3: 커밋**

```bash
git add scripts/tests/frame-grid-tiers.test.js renderer/scripts/modules/frame-grid-tiers.js package.json
git commit -m "feat: 프레임 격자 단계 결정 순수 함수 + 단위 테스트"
```

### Task 2.3: timeline.js 격자 렌더러 교체

**Files:**
- Modify: `renderer/scripts/modules/timeline.js:770-895`

- [ ] **Step 1: import 추가**

`renderer/scripts/modules/timeline.js` 상단 import 섹션에 추가:

```javascript
import { resolveFrameGridTier, TIER } from './frame-grid-tiers.js';
```

- [ ] **Step 2: `_updateFrameGrid` 교체**

기존 `_updateFrameGrid` (라인 776-817)을 다음으로 교체:

```javascript
_updateFrameGrid() {
  if (!this.tracksContainer || this.duration === 0 || this.totalFrames === 0) return;

  // 격자 토글 OFF면 즉시 숨김 후 리턴
  if (!this.gridVisible) {
    if (this.frameGridContainer) {
      this.frameGridContainer.style.display = 'none';
    }
    this._lastFrameGridTier = null;
    return;
  }

  // 프레임 그리드 컨테이너 생성 또는 가져오기
  if (!this.frameGridContainer) {
    this.frameGridContainer = document.createElement('div');
    this.frameGridContainer.className = 'frame-grid-container';
    this.frameGridContainer.style.cssText = `
      position: absolute; top: 0; left: 0;
      width: 100%; height: 100%;
      pointer-events: none; z-index: 1;
    `;
    this.tracksContainer.appendChild(this.frameGridContainer);
  }

  const containerWidth = this.tracksContainer.offsetWidth;
  const frameWidth = containerWidth / this.totalFrames;

  const tierResult = resolveFrameGridTier(frameWidth, this._lastFrameGridTier ?? null);
  this._lastFrameGridTier = tierResult.tier;

  this._renderFrameGridTiered(frameWidth, tierResult);
}
```

- [ ] **Step 3: `_renderFrameGrid` 제거 + `_renderFrameGridTiered` 추가**

기존 `_renderFrameGrid` (라인 824-895) 전체 삭제. 같은 위치에 신규:

```javascript
/**
 * 단계별 프레임 격자 렌더링 — resolveFrameGridTier 결과를 CSS background로 합성
 */
_renderFrameGridTiered(pxPerFrame, t) {
  this.frameGridContainer.style.display = 'block';

  const fps = this.fps || 24;
  const secPx = pxPerFrame * fps;

  // 색상 정의
  const colorSec = getComputedStyle(document.documentElement)
    .getPropertyValue('--accent-shadow-strong').trim() || 'rgba(255, 208, 0, 0.75)';
  const colorHalf = 'rgba(120, 180, 255, 0.55)';
  const colorQuarter = 'rgba(120, 180, 255, 0.32)';
  const colorFrame = 'rgba(255, 255, 255, 0.22)';

  const layers = [];
  const sizes = [];

  if (t.showFiveSec) {
    // 5초 간격 2px 폭 강조선
    const fiveSecPx = secPx * 5;
    layers.push(`repeating-linear-gradient(to right, ${colorSec} 0px, ${colorSec} 2px, transparent 2px, transparent ${fiveSecPx}px)`);
    sizes.push(`${fiveSecPx}px 100%`);
  }
  if (t.showOneSec) {
    layers.push(`repeating-linear-gradient(to right, ${colorSec} 0px, ${colorSec} 2px, transparent 2px, transparent ${secPx}px)`);
    sizes.push(`${secPx}px 100%`);
  }
  if (t.showHalf) {
    const halfSecPx = secPx / 2;
    layers.push(`repeating-linear-gradient(to right, ${colorHalf} 0px, ${colorHalf} 1px, transparent 1px, transparent ${halfSecPx}px)`);
    sizes.push(`${halfSecPx}px 100%`);
  }
  if (t.showQuarter) {
    // ¼초는 fps 인식: round(fps/4) 프레임마다
    const quarterFrames = Math.max(1, Math.round(fps / 4));
    const quarterPx = pxPerFrame * quarterFrames;
    layers.push(`repeating-linear-gradient(to right, ${colorQuarter} 0px, ${colorQuarter} 1px, transparent 1px, transparent ${quarterPx}px)`);
    sizes.push(`${quarterPx}px 100%`);
  }
  if (t.showFrame) {
    layers.push(`repeating-linear-gradient(to right, ${colorFrame} 0px, ${colorFrame} 1px, transparent 1px, transparent ${pxPerFrame}px)`);
    sizes.push(`${pxPerFrame}px 100%`);
  }

  this.frameGridContainer.style.backgroundImage = layers.join(', ');
  this.frameGridContainer.style.backgroundSize = sizes.join(', ');
  this.frameGridContainer.style.backgroundRepeat = 'repeat-x';
  this.frameGridContainer.style.backgroundPosition = '0 0';
}
```

- [ ] **Step 4: 생성자에 상태 필드 추가**

`timeline.js`의 `class Timeline extends EventTarget` 생성자 블록(파일 시작부 `constructor()` 메서드)에 기존 `this.tracksContainer = null;` 같은 필드 정의 근처 위치를 찾는다:

```bash
grep -n "constructor()" renderer/scripts/modules/timeline.js | head -3
```

해당 constructor 내부, `this.frameGridContainer = null;`(또는 이와 유사한 초기화 블록) 라인 **직후**에 다음 두 줄을 추가:

```javascript
this.gridVisible = true;          // 격자 표시 토글 (기본 ON)
this._lastFrameGridTier = null;  // 히스테리시스용 직전 단계
```

추가 후 `git diff renderer/scripts/modules/timeline.js` 로 constructor 블록에 두 필드가 들어간 것을 확인.

- [ ] **Step 5: setter 메서드 추가**

Timeline 클래스에 메서드 추가 (기존 `_updateFrameGrid` 아래, 공개 메서드 인접 위치):

```javascript
/**
 * 격자 표시 토글 설정
 */
setGridVisible(visible) {
  this.gridVisible = !!visible;
  this._updateFrameGrid();
}
```

- [ ] **Step 5b: 파일/duration 변경 시 히스테리시스 리셋**

`timeline.js`에서 비디오 메타데이터(`duration`, `fps`, `totalFrames`)를 설정하는 지점을 찾는다:

```bash
grep -n "this.totalFrames = \|this.duration = \|this.fps = " renderer/scripts/modules/timeline.js | head -5
```

해당 대입부 직후에 한 줄 추가 (중복 추가해도 무해):

```javascript
this._lastFrameGridTier = null;  // 재로드 시 히스테리시스 상태 리셋
```

이유: 직전 파일의 tier 상태를 그대로 들고 있으면 새 파일에서 경계 깜빡임이 발생할 수 있음.

- [ ] **Step 6: lint 및 dev 모드 스모크 테스트**

Run: `npm run lint && npm run dev`

Expected:
  - lint: 새 라인 에러 없음
  - dev 모드: 앱 시작, 타임라인에 기존처럼 격자가 보임 (토글 OFF 없이)

- [ ] **Step 7: 시각적 회귀 체크**

비디오 파일 하나 열고 휠 줌 0.2x → 최대로 스윕:
  - 모든 줌 구간에서 격자가 자연스럽게 등장·진화
  - 깜빡임 없음 (히스테리시스 효과)
  - 이전에는 보이지 않던 ½초/¼초 선이 중간 줌에서 나타남

### Task 2.4: 격자 토글 버튼 추가 (HTML + CSS)

**Files:**
- Modify: `renderer/index.html:716-727`
- Modify: `renderer/styles/main.css`

- [ ] **Step 1: HTML 버튼 삽입**

`renderer/index.html` **L716** (`<div class="timeline-toolbar-right">`) 바로 다음 줄, **L717** (`<div class="zoom-buttons">`) **앞**에 버튼을 삽입. 결과적으로 `.timeline-toolbar-right`의 첫 번째 자식이 되어야 한다. 들여쓰기는 L717과 동일(공백 12칸):

```html
            <button class="timeline-toolbar-btn active" id="btnGridToggle" type="button"
                    aria-label="프레임 격자 표시/숨김" aria-pressed="true"
                    title="프레임 격자 표시/숨김 (기본 ON)">
              <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="3" y1="9" x2="21" y2="9"/>
                <line x1="3" y1="15" x2="21" y2="15"/>
                <line x1="9" y1="3" x2="9" y2="21"/>
                <line x1="15" y1="3" x2="15" y2="21"/>
              </svg>
            </button>
```

확인: `grep -n "btnGridToggle" renderer/index.html` 으로 삽입 확인. 결과가 1건이고 `.timeline-toolbar-right` 안쪽에 위치해야 한다.

- [ ] **Step 2: CSS 스타일 추가**

`renderer/styles/main.css` 적절한 위치 (e.g., `.zoom-buttons` 스타일 근처)에 추가:

```css
.timeline-toolbar-btn {
  background: transparent;
  border: 1px solid var(--border-default, #333);
  border-radius: 4px;
  color: var(--text-secondary, #aaa);
  padding: 4px 8px;
  cursor: pointer;
  transition: all 0.15s ease;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 28px;
  margin-right: 6px;
}

.timeline-toolbar-btn:hover {
  background: var(--bg-elevated, #222);
  color: var(--text-primary, #fff);
  border-color: var(--accent-primary, #ffd000);
}

.timeline-toolbar-btn.active {
  background: rgba(255, 208, 0, 0.12);
  border-color: var(--accent-primary, #ffd000);
  color: var(--accent-primary, #ffd000);
}
```

- [ ] **Step 3: dev 모드에서 버튼 표시 확인**

Run: `npm run dev`
- 타임라인 우측 줌 버튼 왼쪽에 격자 아이콘 버튼 표시
- 호버 시 노란 테두리
- 기본 상태가 `.active` 클래스로 노란색

### Task 2.5: 토글 상태 영속화 (user-settings.js)

**Files:**
- Modify: `renderer/scripts/modules/user-settings.js:161-186, 520+`

- [ ] **Step 1: 기본 설정에 필드 추가**

`renderer/scripts/modules/user-settings.js:161-186`의 `this.settings = { ... }` 객체에 추가 (다른 show* 옵션 근처):

```javascript
// 타임라인 프레임 격자 표시 여부
showFrameGrid: true,
```

- [ ] **Step 2: getter/setter 메서드 추가**

같은 파일 520줄 이후(setShowToastNotifications 근처)에 추가:

```javascript
/**
 * 타임라인 프레임 격자 표시 여부
 */
getShowFrameGrid() {
  return this.settings.showFrameGrid !== false; // 명시적 false일 때만 OFF
}

setShowFrameGrid(show) {
  this.settings.showFrameGrid = !!show;
  this._save();
  this._emit('showFrameGridChanged', { show: this.settings.showFrameGrid });
  log.info('프레임 격자 설정 변경됨', { show });
}
```

### Task 2.6: 토글 버튼 이벤트 연결 (app.js)

**Files:**
- Modify: `renderer/scripts/app.js` (초기화 블록 근처)

- [ ] **Step 1: 버튼 엘리먼트 참조 추가**

`app.js`의 `elements` 객체 정의부 위치 확인:

```bash
grep -n "btnDrawMode:\s*document" renderer/scripts/app.js
```

해당 라인 바로 다음 줄에 같은 들여쓰기로 추가:

```javascript
btnGridToggle: document.getElementById('btnGridToggle'),
```

`git diff renderer/scripts/app.js` 로 elements 객체에만 한 줄 추가됐는지 확인.

- [ ] **Step 2: 초기 상태 동기화 + 클릭 핸들러**

`app.js`에서 timeline 인스턴스 초기화가 완료된 지점(`timeline = new Timeline(...)` 또는 `timeline.setCommentTrack(...)` 이후)을 찾는다:

```bash
grep -n "timeline\.setCommentTrack\|new Timeline" renderer/scripts/app.js | head -3
```

해당 초기화 블록 아래 또는 `userSettings.waitForReady()` 완료 콜백 내부에 다음 블록 추가:

```javascript
// ===== Phase 2: 격자 토글 UI 배선 =====
function _syncGridToggleUI(visible) {
  elements.btnGridToggle.classList.toggle('active', visible);
  elements.btnGridToggle.setAttribute('aria-pressed', String(visible));
}

const initGridVisible = userSettings.getShowFrameGrid();
timeline.setGridVisible(initGridVisible);
_syncGridToggleUI(initGridVisible);

elements.btnGridToggle.addEventListener('click', () => {
  const next = !userSettings.getShowFrameGrid();
  userSettings.setShowFrameGrid(next);
  timeline.setGridVisible(next);
  _syncGridToggleUI(next);
});
```

`aria-pressed`를 상태와 함께 갱신해 접근성 일관성을 확보.

- [ ] **Step 3: 수동 스모크 테스트**

Run: `npm run dev`
- 토글 버튼 클릭 → 격자 완전히 숨김, 버튼 비활성 상태
- 다시 클릭 → 격자 복귀, 버튼 활성 상태
- 앱 종료 후 재실행 → 토글 상태가 유지되는지 (영속화 확인)

### Task 2.7a: 격자 로직 교체 중간 커밋

- [ ] **Step 1: lint + 테스트**

Run: `npm run lint && npm run test:frame-grid`
Expected: 에러 없음, 11개 테스트 PASS.

- [ ] **Step 2: 격자 로직만 먼저 커밋**

timeline.js 격자 로직 교체는 토글 UI와 롤백 단위가 다르므로 분리 커밋한다.

```bash
git add renderer/scripts/modules/timeline.js
git commit -m "refactor: 프레임 격자를 5단 계단식 + 히스테리시스로 교체

기존 3단 이진 전환(2px/4px)을 제거하고 resolveFrameGridTier
순수 함수 기반으로 재구현. 토글 UI는 후속 커밋에서 배선."
```

### Task 2.7b: 토글 UI 배선 + 설정 영속화 커밋

- [ ] **Step 1: 수동 회귀 스모크**

Run: `npm run dev`

- 토글 버튼 클릭으로 격자 숨김/표시
- 앱 종료 → 재시작 → 마지막 상태 복원
- `aria-pressed`가 DevTools에서 "true"/"false"로 갱신되는지

- [ ] **Step 2: UI/설정 커밋**

```bash
git add renderer/index.html renderer/styles/main.css renderer/scripts/modules/user-settings.js renderer/scripts/app.js
git commit -m "$(cat <<'EOF'
feat: 격자 토글 버튼 UI + userSettings.showFrameGrid 영속화

- timeline-toolbar-right에 aria-pressed를 지원하는 토글 버튼
- 설정 변경 시 showFrameGridChanged 이벤트 emit
- 앱 재시작 시 토글 상태 복원

Phase 2 완료. 관련 단계: Phase 1 (B 토글), Phase 3 (코멘트 중첩).
EOF
)"
```

### Phase 2 엣지케이스 & 회귀 방지

- [ ] **케이스 1**: 24fps / 25fps / 30fps 비디오 각각 로드
  - 기대: ¼초 선이 각각 6f / 6f / 8f 간격으로 표시
- [ ] **케이스 2**: duration 0 또는 totalFrames 0 비디오
  - 기대: `_updateFrameGrid` 초기 guard로 크래시 없음
- [ ] **케이스 3**: 매우 짧은 비디오 (<1초)
  - 기대: 5초 강조선 없음, 1초 이상 줌에서만 1초 선 보임
- [ ] **케이스 4**: 매우 긴 비디오 (>30분) 최대 줌아웃
  - 기대: 5초 강조선 렌더 성능 이슈 없음 (CSS gradient이므로 O(1))
- [ ] **케이스 5**: 라이트 모드로 전환 시
  - 기대: 격자 색상이 배경과 구분되는지 (필요 시 라이트 모드용 색상 추가)
- [ ] **케이스 6**: 토글 OFF 상태에서 줌 변경
  - 기대: `_updateFrameGrid`가 바로 return해 불필요한 렌더 없음
- [ ] **케이스 7**: 토글 OFF → 앱 재시작 → 복원 확인
  - 기대: 버튼 비활성, 격자 숨김 상태로 시작
- [ ] **케이스 8**: Liveblocks 협업 — 격자 토글은 로컬 설정이므로 다른 사용자에게 영향 없음
  - 기대: 본인 화면만 영향

### Phase 2 롤백

변경이 여러 파일에 걸쳐 있으므로 단일 커밋 revert:

```bash
git revert <phase2-commit-hash>
# frame-grid-tiers 모듈 커밋도 같이
git revert <phase2-tiers-commit-hash>
```

만약 일부만 롤백해야 한다면 (예: 단계 로직은 OK, 토글만 제거):
```bash
git checkout <phase2-commit-hash>^ -- renderer/index.html renderer/scripts/app.js renderer/styles/main.css
# user-settings.js의 showFrameGrid 필드만 수동 제거
```

---

## Chunk 3 — Phase 3: 코멘트 중첩 하이브리드 (Mode C)

### 단계 목표
1. 순수 함수 `findRangeClusters(comments)`와 `assignLanes(cluster)` 생성 (단위 테스트 포함)
2. `renderCommentRanges` 확장: 클러스터 탐지 → 접힘(배지) / 펼침(레인) 두 상태 렌더
3. 배지 클릭 시 애니메이션 펼침, "접기" 칩으로 복귀
4. MVP 제외 사항: 자동 접힘, 점 댓글과의 혼합 클러스터링

### Task 3.1 (TDD): 클러스터링/레인 할당 — 테스트 작성

**Files:**
- Create: `scripts/tests/comment-cluster.test.js`

- [ ] **Step 1: 테스트 파일 생성**

`scripts/tests/comment-cluster.test.js`:

```javascript
/**
 * comment-cluster 단위 테스트
 * 실행: npm run test:cluster
 */
const { test } = require('node:test');
const assert = require('node:assert/strict');

let mod;
test('모듈 로드', async () => {
  mod = await import('../../renderer/scripts/modules/comment-cluster.js');
});

function c(id, start, end) {
  return { markerId: id, startFrame: start, endFrame: end };
}

// --- findRangeClusters ---

test('빈 배열 → 빈 클러스터', () => {
  assert.deepEqual(mod.findRangeClusters([]), []);
});

test('단일 댓글 → 단일 클러스터 (크기 1)', () => {
  const cs = [c('a', 0, 10)];
  const result = mod.findRangeClusters(cs);
  assert.equal(result.length, 1);
  assert.equal(result[0].length, 1);
});

test('비중첩 둘 → 독립 클러스터 2개', () => {
  const cs = [c('a', 0, 10), c('b', 20, 30)];
  const result = mod.findRangeClusters(cs);
  assert.equal(result.length, 2);
});

test('인접하지만 겹치지 않음 (end == start): 독립', () => {
  const cs = [c('a', 0, 10), c('b', 10, 20)];
  const result = mod.findRangeClusters(cs);
  assert.equal(result.length, 2, 'endFrame == startFrame은 겹침 아님 (반열린 구간 규칙)');
});

test('정확히 겹치는 두 댓글: 하나의 클러스터', () => {
  const cs = [c('a', 0, 10), c('b', 5, 15)];
  const result = mod.findRangeClusters(cs);
  assert.equal(result.length, 1);
  assert.equal(result[0].length, 2);
});

test('전이적 겹침 (A-B, B-C): 단일 클러스터로 병합', () => {
  const cs = [c('a', 0, 10), c('b', 5, 15), c('c', 12, 20)];
  const result = mod.findRangeClusters(cs);
  assert.equal(result.length, 1);
  assert.equal(result[0].length, 3);
});

test('정렬되지 않은 입력 — 결과는 정렬됨', () => {
  const cs = [c('c', 20, 30), c('a', 0, 10), c('b', 5, 15)];
  const result = mod.findRangeClusters(cs);
  // 정렬 후 [a,b], [c]
  assert.equal(result.length, 2);
  assert.equal(result[0][0].markerId, 'a');
  assert.equal(result[1][0].markerId, 'c');
});

test('동일 startFrame: 단일 클러스터', () => {
  const cs = [c('a', 10, 20), c('b', 10, 30)];
  const result = mod.findRangeClusters(cs);
  assert.equal(result.length, 1);
});

// --- assignLanes ---

test('단일 댓글: 레인 0', () => {
  const cs = [c('a', 0, 10)];
  const { lanes, maxLane } = mod.assignLanes(cs);
  assert.equal(lanes.length, 1);
  assert.equal(maxLane, 1);
  assert.equal(cs[0]._lane, 0);
});

test('비중첩 둘: 둘 다 레인 0 (같은 레인에 순차)', () => {
  const cs = [c('a', 0, 10), c('b', 20, 30)];
  const { lanes, maxLane } = mod.assignLanes(cs);
  assert.equal(maxLane, 1);
  assert.equal(cs[0]._lane, 0);
  assert.equal(cs[1]._lane, 0);
});

test('완전 겹침 둘: 레인 0, 1', () => {
  const cs = [c('a', 0, 10), c('b', 5, 15)];
  const { lanes, maxLane } = mod.assignLanes(cs);
  assert.equal(maxLane, 2);
  assert.equal(cs[0]._lane, 0);
  assert.equal(cs[1]._lane, 1);
});

test('세 개 겹침: 레인 0, 1, 2', () => {
  const cs = [c('a', 0, 10), c('b', 5, 15), c('c', 8, 20)];
  const { maxLane } = mod.assignLanes(cs);
  assert.equal(maxLane, 3);
});

test('재사용 가능한 레인: A(0-10), B(5-15), C(12-20) → A,C는 레인 0 공유', () => {
  const cs = [c('a', 0, 10), c('b', 5, 15), c('c', 12, 20)];
  const { maxLane } = mod.assignLanes(cs);
  assert.equal(maxLane, 2, 'B는 새 레인, C는 A의 레인 재사용');
  assert.equal(cs[0]._lane, 0);
  assert.equal(cs[1]._lane, 1);
  assert.equal(cs[2]._lane, 0);
});
```

- [ ] **Step 2: package.json 스크립트**

`package.json`의 `scripts`에 추가:

```json
"test:cluster": "node --test scripts/tests/comment-cluster.test.js",
```

- [ ] **Step 3: 테스트 실행 — 실패 확인**

Run: `npm run test:cluster`
Expected: `Cannot find module ...comment-cluster.js` 실패

### Task 3.2 (TDD): 클러스터링/레인 함수 구현

**Files:**
- Create: `renderer/scripts/modules/comment-cluster.js`

- [ ] **Step 1: 모듈 생성**

`renderer/scripts/modules/comment-cluster.js`:

```javascript
/**
 * 구간 댓글 클러스터링 + 레인 할당 — 순수 함수
 *
 * 구간: [startFrame, endFrame) 반열린. endFrame == 다른 startFrame은 겹침 아님.
 */

/**
 * 시간 구간이 겹치는 댓글을 연결 성분으로 묶음.
 * @param {Array<{markerId, startFrame, endFrame}>} comments
 * @returns {Array<Array>} 클러스터 배열 (각 클러스터는 startFrame 오름차순)
 */
export function findRangeClusters(comments) {
  if (!Array.isArray(comments) || comments.length === 0) return [];

  const sorted = [...comments].sort((a, b) => a.startFrame - b.startFrame);
  const clusters = [];
  let current = [];
  let currentMaxEnd = -Infinity;

  for (const c of sorted) {
    if (current.length === 0 || c.startFrame < currentMaxEnd) {
      current.push(c);
      currentMaxEnd = Math.max(currentMaxEnd, c.endFrame);
    } else {
      clusters.push(current);
      current = [c];
      currentMaxEnd = c.endFrame;
    }
  }
  if (current.length) clusters.push(current);
  return clusters;
}

/**
 * 그리디 레인 할당 — 겹치지 않는 댓글은 같은 레인 재사용.
 * @param {Array<{startFrame, endFrame}>} cluster
 * @returns {{lanes: Array<Array>, maxLane: number}}
 *   각 comment에 `_lane` (0-기반) 속성이 붙음 (mutation).
 */
export function assignLanes(cluster) {
  const sorted = [...cluster].sort((a, b) => a.startFrame - b.startFrame);
  const laneEnds = []; // laneEnds[i] = 해당 레인의 마지막 endFrame

  for (const c of sorted) {
    let assigned = -1;
    for (let i = 0; i < laneEnds.length; i++) {
      if (laneEnds[i] <= c.startFrame) { // 반열린: 이전 레인의 end가 현재 start와 같거나 작으면 재사용
        assigned = i;
        break;
      }
    }
    if (assigned === -1) {
      assigned = laneEnds.length;
      laneEnds.push(c.endFrame);
    } else {
      laneEnds[assigned] = c.endFrame;
    }
    c._lane = assigned;
  }

  // 클러스터 원본에도 _lane 반영 (sorted는 얕은 복제)
  return { lanes: laneEnds, maxLane: laneEnds.length };
}

/**
 * 클러스터 고유 식별자 — 첫(가장 이른 start) 댓글의 markerId 사용.
 */
export function clusterKey(cluster) {
  return cluster.length > 0 ? cluster[0].markerId : null;
}
```

**주의**: renderer의 다른 모듈은 모두 `export` (ES module)을 쓴다. 테스트는 `import()` dynamic import로 불러오므로 CJS/ESM 혼용 문제 없음.

- [ ] **Step 2: 테스트 실행 — 통과 확인**

Run: `npm run test:cluster`
Expected: 14개 테스트 전부 PASS.

- [ ] **Step 3: 커밋**

```bash
git add scripts/tests/comment-cluster.test.js renderer/scripts/modules/comment-cluster.js package.json
git commit -m "feat: 구간 댓글 클러스터링 + 레인 할당 순수 함수 + 단위 테스트"
```

### Task 3.3: timeline.js — renderCommentRanges 클러스터 배지 렌더 추가

**Files:**
- Modify: `renderer/scripts/modules/timeline.js:1918-2003`

- [ ] **Step 1: import 추가**

`timeline.js` 상단에:

```javascript
import { findRangeClusters, assignLanes, clusterKey } from './comment-cluster.js';
```

- [ ] **Step 2: 상태 필드 추가**

Timeline 클래스 생성자에:

```javascript
this.expandedClusterId = null; // 현재 펼친 클러스터의 키 (markerId)
```

- [ ] **Step 3: `renderCommentRanges` 확장**

기존 `renderCommentRanges(comments)` (L1918-1944)을 다음으로 교체:

```javascript
renderCommentRanges(comments) {
  if (!this.commentTrack) return;

  // 데이터 캐시 (펼침/접힘 재렌더용)
  this._lastComments = comments;

  // 기존 댓글 제거
  this.commentTrack.innerHTML = '';

  // 댓글이 없으면 트랙 및 레이어 헤더 숨김 (기존 로직 유지)
  if (!comments || comments.length === 0) {
    this.commentTrack.style.display = 'none';
    if (this.commentLayerHeader) {
      this.commentLayerHeader.style.display = 'none';
    }
    // 펼침 상태는 데이터가 없어진 상황이므로 리셋
    this.expandedClusterId = null;
    return;
  }

  // 트랙 및 레이어 헤더 표시 (기존 로직 유지)
  this.commentTrack.style.display = 'block';
  if (this.commentLayerHeader) {
    this.commentLayerHeader.style.display = 'flex';
  }

  // 1) 클러스터링
  const clusters = findRangeClusters(comments);

  // 2) 펼침 상태 유효성 검증 — 해당 clusterKey가 더 이상 존재하지 않으면 리셋
  if (this.expandedClusterId !== null) {
    const stillExists = clusters.some(c =>
      clusterKey(c) === this.expandedClusterId && c.length > 1
    );
    if (!stillExists) {
      this.expandedClusterId = null;
    }
  }

  // 3) 최대 레인 수 계산 — 펼친 클러스터가 있으면 그 클러스터의 레인 수
  let maxLanes = 1;
  clusters.forEach(cluster => {
    if (clusterKey(cluster) === this.expandedClusterId && cluster.length > 1) {
      const { maxLane } = assignLanes(cluster);
      maxLanes = Math.max(maxLanes, maxLane);
    }
  });

  // 4) 트랙 높이 동적 설정 (애니메이션)
  const baseHeight = 24;
  const laneHeight = 24;
  const lanePadding = 6;
  const targetHeight = maxLanes > 1
    ? laneHeight * maxLanes + lanePadding
    : baseHeight;
  this.commentTrack.style.height = `${targetHeight}px`;

  // 5) 각 클러스터 렌더
  clusters.forEach(cluster => {
    const key = clusterKey(cluster);
    if (cluster.length === 1) {
      // 단일 댓글 — 기존처럼
      const el = this._createCommentRangeElement(cluster[0]);
      el.style.top = '2px';
      this.commentTrack.appendChild(el);
    } else if (key === this.expandedClusterId) {
      // 펼친 클러스터 — 레인별 렌더 + 접기 칩
      assignLanes(cluster);
      cluster.forEach(c => {
        const el = this._createCommentRangeElement(c);
        el.style.top = `${2 + c._lane * laneHeight}px`;
        this.commentTrack.appendChild(el);
      });
      const chip = this._createCollapseChip(cluster, maxLanes);
      this.commentTrack.appendChild(chip);
    } else {
      // 접힌 클러스터 — 배지
      const badge = this._createClusterBadgeElement(cluster);
      this.commentTrack.appendChild(badge);
    }
  });
}
```

- [ ] **Step 4: `_createClusterBadgeElement` 추가**

`_createCommentRangeElement` 직후에 추가:

```javascript
/**
 * 접힌 클러스터의 배지 바 DOM 생성
 */
_createClusterBadgeElement(cluster) {
  const totalFrames = this.totalFrames || 1;
  const minStart = Math.min(...cluster.map(c => c.startFrame));
  const maxEnd = Math.max(...cluster.map(c => c.endFrame));
  const leftPercent = (minStart / totalFrames) * 100;
  const widthPercent = ((maxEnd - minStart) / totalFrames) * 100;

  const el = document.createElement('div');
  el.className = 'comment-cluster-badge';
  el.dataset.clusterKey = clusterKey(cluster);
  el.style.left = `${leftPercent}%`;
  el.style.width = `${Math.max(widthPercent, 3)}%`;
  el.style.top = '2px';

  // 스택 힌트 (최대 3색)
  const hint = document.createElement('div');
  hint.className = 'comment-cluster-stack-hint';
  cluster.slice(0, 3).forEach(c => {
    const stripe = document.createElement('div');
    stripe.style.background = c.color || '#4a9eff';
    hint.appendChild(stripe);
  });
  el.appendChild(hint);

  // 카운트 칩
  const chip = document.createElement('span');
  chip.className = 'comment-cluster-count';
  chip.textContent = `⊕${cluster.length}`;
  el.appendChild(chip);

  // 라벨
  const label = document.createElement('span');
  label.className = 'comment-cluster-label';
  label.textContent = '겹친 코멘트';
  el.appendChild(label);

  // 클릭 → 펼치기
  el.addEventListener('click', (e) => {
    e.stopPropagation();
    this.expandedClusterId = clusterKey(cluster);
    this.renderCommentRanges(this._lastComments || []);
  });

  return el;
}

/**
 * 펼친 클러스터를 접는 칩 DOM
 */
_createCollapseChip(cluster, maxLanes) {
  const totalFrames = this.totalFrames || 1;
  const minStart = Math.min(...cluster.map(c => c.startFrame));
  const leftPercent = (minStart / totalFrames) * 100;

  const chip = document.createElement('button');
  chip.className = 'comment-collapse-chip';
  chip.type = 'button';
  chip.textContent = '▲ 접기';
  chip.style.left = `${leftPercent}%`;
  chip.style.top = `${24 * maxLanes + 2}px`;

  chip.addEventListener('click', (e) => {
    e.stopPropagation();
    this.expandedClusterId = null;
    this.renderCommentRanges(this._lastComments || []);
  });

  return chip;
}
```

- [ ] **Step 5: 외부 클릭으로 접기**

`setCommentTrack(trackElement)` 혹은 초기화 부근에 전역 클릭 리스너 등록:

```javascript
// 타임라인 배경 클릭 시 펼친 클러스터 접기
this.commentTrack.addEventListener('click', (e) => {
  if (e.target === this.commentTrack && this.expandedClusterId !== null) {
    this.expandedClusterId = null;
    this.renderCommentRanges(this._lastComments || []);
  }
});
```

- [ ] **Step 6: `_lastComments` 캐시 확인**

Step 3의 `renderCommentRanges` 교체 블록에 이미 `this._lastComments = comments;`가 함수 진입부에 포함돼 있다. 배지/칩 클릭 핸들러는 `this._lastComments || []`로 재호출한다.

**주의**: 이 캐시는 "마지막으로 본 렌더 입력"이므로 Liveblocks로 비동기 업데이트가 들어오는 동안 약간 스테일할 수 있다. 일반 흐름에서는 commentManager가 `renderCommentRanges()`를 새 데이터로 재호출하므로 곧 갱신되지만, 펼침 중 수정이 들어오면:
  - "펼침 상태 유효성 검증"(Step 3의 "2) ...")에서 clusterKey가 없어지면 자동 접힘
  - 그 외에는 다음 commentManager 업데이트 한 사이클 안에 최신화

향후 필요 시 `commentManager.on('update', ...)` 이벤트 구독으로 개선할 수 있음(후속 최적화).

### Task 3.4: CSS — 클러스터 배지 + 애니메이션

**Files:**
- Modify: `renderer/styles/main.css:2865-2971`

- [ ] **Step 1: comment-track 높이 transition 추가**

`renderer/styles/main.css:2865`의 `.comment-track` 규칙에 추가:

```css
.comment-track {
  /* ... 기존 속성 ... */
  transition: height 0.28s ease;
  position: relative; /* 기존에 없다면 */
}
```

- [ ] **Step 2: 배지/칩 스타일 추가**

`.comment-range-item` 규칙 직후에 추가:

```css
/* Phase 3 — 클러스터 배지 (접힘) */
.comment-cluster-badge {
  position: absolute;
  height: 20px;
  border-radius: 3px;
  background: linear-gradient(180deg, rgba(245,158,11,0.9), rgba(217,119,6,0.85));
  border-left: 2px solid var(--warning, #f59e0b);
  border-right: 2px solid var(--warning, #f59e0b);
  box-shadow: 0 2px 6px rgba(0,0,0,0.5), inset 0 0 0 1px rgba(255,255,255,0.08);
  cursor: pointer;
  display: flex;
  align-items: center;
  padding: 0 10px 0 8px;
  gap: 6px;
  font-size: 10px;
  color: #fff;
  font-weight: 600;
  overflow: hidden;
  white-space: nowrap;
  transition: filter 0.15s, box-shadow 0.15s;
}

.comment-cluster-badge:hover {
  filter: brightness(1.15);
  box-shadow: 0 2px 10px rgba(245,158,11,0.35), inset 0 0 0 1px rgba(255,255,255,0.15);
}

.comment-cluster-stack-hint {
  display: flex;
  flex-direction: column;
  gap: 1px;
  width: 3px;
  height: 14px;
  flex-shrink: 0;
}

.comment-cluster-stack-hint > div {
  flex: 1;
  border-radius: 1px;
}

.comment-cluster-count {
  background: rgba(0,0,0,0.35);
  padding: 1px 6px;
  border-radius: 8px;
  font-family: ui-monospace, SFMono-Regular, monospace;
  font-size: 10px;
  flex-shrink: 0;
}

.comment-cluster-label {
  overflow: hidden;
  text-overflow: ellipsis;
}

/* 접기 칩 (펼친 상태) */
.comment-collapse-chip {
  position: absolute;
  background: rgba(60, 60, 60, 0.85);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 3px;
  padding: 2px 8px;
  font-size: 9px;
  color: #ccc;
  cursor: pointer;
  font-family: ui-monospace, monospace;
  height: 16px;
  z-index: 20;
  transform: translateY(0);
  transition: background 0.15s, color 0.15s;
}

.comment-collapse-chip:hover {
  background: rgba(80, 80, 80, 0.9);
  color: #fff;
}

/* 구간 바의 top 전환 (펼침/접힘 애니메이션) */
/* 주의: 기존 main.css:2894에 이미
   `transition: opacity var(--transition-fast), box-shadow var(--transition-fast);`
   가 있음. 별도 블록을 추가하기보다 **기존 규칙을 수정**해 병합한다: */
```

**기존 `.comment-range-item` 블록 수정 (main.css:2894)**:
```css
/* before */
transition: opacity var(--transition-fast), box-shadow var(--transition-fast);

/* after (top 추가) */
transition: opacity var(--transition-fast), box-shadow var(--transition-fast), top 0.28s ease;
```

이로써 중복 규칙을 피하고, 기존 opacity/shadow 전환과 펼침 애니메이션이 하나의 `transition` 선언으로 통합된다.

### Task 3.5: 수동 테스트 (dev 모드)

- [ ] **Step 1: dev 모드 실행**

Run: `npm run dev`
비디오 파일 하나 열고 타임라인에 구간 코멘트 3개 추가 (1,2번은 시간 겹치게).

- [ ] **Step 2: 클러스터 배지 확인**

- 겹친 영역에 주황 배지가 표시되는지
- 배지 좌측에 3색 스택 힌트가 보이는지 (색은 작성자 별)
- "⊕3 겹친 코멘트" 텍스트

- [ ] **Step 3: 배지 클릭 → 펼침 확인**

- 트랙 높이가 24px → 54px 정도로 부드럽게 증가 (0.28s)
- 3개 댓글이 레인 0, 1, (필요 시 2)로 분리
- 각 댓글 호버 시 좌우 핸들 노출
- 아래에 "▲ 접기" 칩 표시

- [ ] **Step 4: 펼친 상태에서 리사이즈**

- 특정 댓글의 우측 핸들을 드래그해서 길이 늘림
- 다른 레인의 댓글과 시각적 충돌 없음 (다른 레인이므로 겹쳐도 별개)
- `setDuration()`/`endFrame` 업데이트가 실제로 반영되는지 확인
- Liveblocks로 다른 사용자에게도 동기화되는지 (협업 테스트)

- [ ] **Step 5: 접기**

- "▲ 접기" 클릭 → 트랙 높이 복귀, 배지로 재접힘
- 타임라인 배경 빈 영역 클릭 → 동일 복귀

- [ ] **Step 6: 비중첩 댓글 회귀**

- 겹치지 않는 댓글 1개만 있을 때: 기존과 동일하게 단일 바로 표시 (배지 X)
- 비중첩 댓글은 핸들 접근 문제 없음

### Task 3.6: Phase 3 커밋

- [ ] **Step 1: lint**

Run: `npm run lint`
Expected: 에러 없음

- [ ] **Step 2: 모든 단위 테스트**

Run: `npm run test:frame-grid && npm run test:cluster && npm run test:recent`
Expected: 모두 PASS

- [ ] **Step 3: 커밋**

```bash
git add renderer/scripts/modules/timeline.js renderer/styles/main.css
git commit -m "$(cat <<'EOF'
feat: 구간 코멘트 중첩 시 하이브리드 펼침 UI (Mode C)

중첩된 구간 댓글이 뒤쪽 핸들 접근 불가 문제를 해소.
- 클러스터를 주황 배지로 접어 표시 (스택 힌트 3색 + ⊕N 카운트)
- 배지 클릭 → 트랙 높이 애니메이션으로 확장 + 레인 분리
- "▲ 접기" 칩 또는 빈 영역 클릭으로 복귀
- 점 댓글 픽셀 클러스터링(기존)과 별도 체계

MVP 제외: 재생 헤드 이탈 시 자동 접힘, 점+구간 혼합 클러스터링.

Phase 1, 2에 이어 마지막 Phase. 관련 spec:
DEVLOG/2026-04-17-사용자리뷰-UIUX개선-설계.md
EOF
)"
```

### Phase 3 엣지케이스 & 회귀 방지

- [ ] **케이스 1**: 클러스터에 4개 이상 댓글
  - 기대: 레인 3개 이상 필요 시 트랙 높이 더 커짐, 스택 힌트는 3개까지만 (4번째부터는 "+N" 대체 표기 고려 — 후속 이슈)
- [ ] **케이스 2**: 매우 짧은 구간 (1~2프레임)
  - 기대: `minWidthPercent` 기존 로직 유지, 배지에서도 최소 너비 3% 보장
- [ ] **케이스 3**: 클러스터 내 첫 댓글(markerId 사용하는)이 삭제됨
  - 기대: `expandedClusterId`가 무효화되어 다음 렌더 시 접힘 복귀 (재검증 필요)
  - 구현 확인: `renderCommentRanges` 재호출 시 해당 cluster 재계산되어 key가 바뀌면 자동 접힘
- [ ] **케이스 4**: 펼친 상태에서 Liveblocks로 다른 사용자가 댓글 추가
  - 기대: 레이아웃 재계산, 펼침 상태 유지 (가능하면). 첫 댓글이 유지되는 한 key 동일
- [ ] **케이스 5**: 펼친 상태에서 다른 사용자가 클러스터 내 댓글 삭제
  - 기대: 자동 재렌더. 첫 댓글 삭제 시 접힘. 아니면 펼침 유지
- [ ] **케이스 6**: Resolved 댓글이 클러스터에 섞여 있음
  - 기대: 배지에 자연스럽게 포함. 펼침 상태에서 각자 기존 resolved 스타일(초록 투명)
- [ ] **케이스 7**: 줌 인/아웃으로 pxPerFrame이 극단적으로 작을 때
  - 기대: 배지가 최소 3% 너비로 보장, 클릭 가능
- [ ] **케이스 8**: 펼친 상태에서 사용자가 F(전체화면) 토글 등 앱 상태 전환
  - 기대: 전환 후 재렌더 시 펼침 상태 유지 또는 안전한 접힘

### Phase 3 롤백

```bash
# 최종 UI 커밋만 revert
git revert <phase3-ui-commit-hash>
# comment-cluster 모듈 커밋도 같이
git revert <phase3-cluster-commit-hash>
```

부분 롤백(배지만 유지, 펼침 제거): `expandedClusterId` 무시하고 항상 배지 상태로 렌더. 수동 revert로 timeline.js만 조정.

---

## Cross-Phase 통합 테스트 (세 Phase 완료 후)

- [ ] **T1**: 드로잉 모드 ON + 격자 토글 OFF + 클러스터 펼침
  - 기대: 세 상태가 독립적으로 유지
- [ ] **T2**: 라이트 모드에서 모든 Phase 동작 확인
  - 기대: 색상 시인성 문제 없음 (필요 시 라이트 모드 색 별도)
- [ ] **T3**: Liveblocks 협업 — 사용자 A가 클러스터 펼친 상태에서 사용자 B가 같은 프레임에 댓글 추가
  - 기대: 사용자 A 화면에 즉시 반영, 레이아웃 깨짐 없음
- [ ] **T4**: 앱 재시작
  - 드로잉 모드: 세션 단위, 재시작 시 OFF (기존)
  - 격자 토글: 영속화, 설정값 복원
  - 클러스터 펼침: 세션 단위, 재시작 시 접힘 (의도)
- [ ] **T5**: 기존 .bframe 파일(구버전) 로드
  - 기대: 데이터 모델 불변이므로 스키마 호환. 구간 필드 없는 점 댓글도 정상 렌더
- [ ] **T6**: 빌드 검증 (`npm run build`)
  - 기대: dist/win-unpacked/BFRAME_alpha_v2.exe 실행 시 3 Phase 모두 동작
- [ ] **T7**: ESLint + Prettier
  - Run: `npm run lint && npm run format`
  - Expected: 에러 없음, 포맷 통일

---

## 최종 체크리스트 (PR/배포 전)

- [ ] 모든 단위 테스트 PASS (`npm run test:recent && npm run test:frame-grid && npm run test:cluster`)
- [ ] Lint 통과 (`npm run lint`)
- [ ] Format 적용 (`npm run format`)
- [ ] 개발 빌드 검증 (`npm run build` → `dist/win-unpacked/`에서 실행)
- [ ] Cross-Phase 통합 테스트 T1~T7 완료
- [ ] DEVLOG의 설계 문서와 구현 일치 여부 확인 (clusterId, showFrameGrid 등 명명 일치)
- [ ] 커밋 히스토리 정리 (각 Phase별로 의미 있는 단위)
- [ ] PR 작성 시 스펙 문서 링크 포함

---

## 참고 — 리스크 대응 매트릭스

| Phase | 리스크 | 발생 시 대응 |
|---|---|---|
| 1 | B 키가 다른 컨텍스트에서 충돌 | `matchShortcut`에서 입력 포커스 체크 유지 여부 재확인. 충돌 시 기존 조건 복원 |
| 2 | 단계 경계 깜빡임 (히스테리시스 불충분) | `HYSTERESIS_PCT`를 5% 또는 7%로 상향 조정 |
| 2 | 라이트 모드에서 ½초/¼초 파랑 가독성 저하 | `[data-theme="light"] .frame-grid-container` 스타일로 색 분기 |
| 2 | 매우 큰 pxPerFrame(줌 최대)에서 백그라운드 크기 계산 오차 | CSS `background-size`는 소수점 허용. 문제 없음 |
| 3 | 클러스터 내 댓글 수정/삭제 시 key 갱신 누락 | `renderCommentRanges`에서 현재 `expandedClusterId`에 해당하는 클러스터가 없으면 `null`로 리셋 (guard 추가) |
| 3 | 트랙 높이 변화로 하단 UI 밀림 | 타임라인 상위 레이아웃 재계산. 필요 시 `position: relative`/`absolute` 조정 |
| 3 | 펼친 상태에서 Liveblocks 업데이트 콜백 폭주 | `renderCommentRanges` 내 `requestAnimationFrame` 디바운싱 추가 (후속 최적화) |
