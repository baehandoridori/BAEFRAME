# 이전 리뷰 기능 2.11.0-beta 릴리스

## 범위와 버전

- 사용자가 실제 시연 후 PR 리뷰, 머지, 빌드와 공유드라이브 배포까지 승인했다. Slack 안내 게시 요청은 없다.
- 원격 main `53062c8` 및 기존 배포 버전 2.10.0-beta를 확인했다. 이전 리뷰 조회·이어받기·타임라인 표시를 새 기능으로 제공하므로 2.11.0-beta로 올린다.
- 원본 리뷰는 변경하지 않고 현재 문서의 `reviewCarryoverV1`에 출처와 스냅샷을 저장한다. 원본 미해결/해결 값을 계승하며 이후 상태 변경은 현재 문서에만 적용된다.
- 기능/UI/실제 Electron 조작 근거는 [타임라인 통합 기록](2026-09-08-이전-리뷰-타임라인-통합.md)을 참조한다.

## 배포 전 검증

- 저장 회귀 `npm run test:fabric-drawing-persistence`: **164 pass, 0 fail, 0 cancelled, 0 skipped, exit 0**.
- `test:previous-review`, `test:comment-popout`, `test:comment-input`, `test:cluster`, `test:frame-grid`, `test:version`, `test:playlist`, `test:ux`, `test:mpv`의 중복 파일을 제거하고 순차 실행: **953 pass, 0 fail, 0 cancelled, 0 skipped, exit 0**.
- 변경 JavaScript 전체 ESLint: `--no-ignore --rule 'linebreak-style: off'`, **0 errors, 기존 app.js warning 18개, exit 0**. 저장소 전체 줄바꿈 변환은 하지 않았다.
- 최초 확장 검사는 새 패널 변수를 모르는 영상 전환 하네스, 삭제된 구 주석을 찾는 함수 추출, 고정된 버전 기대값, Windows CRLF에 민감한 문자열 검사 때문에 실패했다. 하네스와 단언을 현재 연결 구조에 맞춰 갱신한 뒤 위 결과를 얻었다.
- 샌드박스 안의 Electron GPU 실행과 Windows 대소문자 구분 디렉터리 검사는 환경 제약으로 실패했다. 실제 권한에서 실행하여 별도 통과를 확인한 뒤 동일 조건으로 전체 회귀를 완료했다.
- 로그: 작업 체크아웃의 `.superpowers/release-2.11.0/` (git 제외).
- PR 대기 중 mpv 표시 정책을 검토하여 새 옵션창 등록 누락을 보완했다. 좁은 댓글 패널에서 옵션창이 영상과 겹치는 경우에만 기존 대화상자와 동일하게 native host를 숨긴다. 숨김/비겹침에서는 차단하지 않는다. 회귀 테스트 RED 1 fail 후 GREEN **38 pass / 0 fail / 0 cancelled, exit 0**, 추가 변경 ESLint와 diff 검사 통과.
- 실제 mpv 격리 앱에서도 표식 클릭으로 **3.001초** 이동, 옵션을 영상 영역과 겹치게 배치했을 때 host 숨김, 닫은 뒤 복귀를 검증했다. `mpv-options-e2e.json`: **ready true, errors 0**. 소스 앱 자동 조작이며 여러 PC 실기는 아니다.

## Codex 리뷰 반영

- PR #219, `d3bf7c9` 리뷰 P2: 첫 `.bframe` 생성 전에 유일한 이어받기 항목을 취소하면 `getItems()`가 tombstone을 숨겨 저장할 내용이 없다고 판단했다.
- `ReviewCarryoverManager.hasStoredItems()`는 표시 항목과 별개로 저장 레코드(취소 포함)를 검사한다. `ReviewDataManager.hasSubstantiveContent()`가 이 값을 사용하여 자동 저장을 유지한다.
- RED 1 fail 재현 후 첫 자동 저장, 재열기, 동일 문서의 오래된 데이터와 병합해도 취소가 유지되는 검증을 추가했다. **135 pass / 0 fail / 0 cancelled / 0 skipped, exit 0**. 변경 파일 ESLint 및 diff 검사 통과.
- `88886c1`의 추가 P2: Liveblocks 연결 시 파일 알림 경로가 drawingsV3만 반영해 이어받기 변경이 다른 클라이언트에 갱신되지 않았다. `reloadReviewCarryoverFromDisk()`를 같은 파일 알림 throttle에 연결했다. 현재 댓글·레거시 드로잉을 건드리지 않고 carryover만 세 갈래로 병합한다. 파일/문서 계보, 지원 버전, 요청 순서와 저장/재로드 기준선 변경을 검사해 늦은 결과를 버린다. 로컬 미저장 편집과 자동 저장은 유지한다.
- 연결 중 파일 동기화 RED 3 fail 재현 후, 다른 사용자의 추가·상태·취소, 읽는 중 로컬 편집, 영상 전환, 새 요청보다 늦은 읽기, 저장 완료 후 도착하는 이전 읽기를 검증했다. 관련 **139 pass / 0 fail / 0 cancelled / 0 skipped, exit 0**, ESLint 0 errors / 기존 18 warnings.

## 릴리스 진행

PR은 최종 head에 대한 Codex 명시적 완료 신호와 미해결 지적 없음을 확인한 뒤 머지한다. 정확한 merge SHA의 깨끗한 별도 체크아웃에서 `npm run build`를 실행한다. 기존 배포는 백업하고 staging을 검증한 뒤 교체한다. 최종 소스/배포 전체 파일 상대 경로·바이트·SHA-256 비교를 별도 배포 증거에 기록한다.

실제 여러 PC의 동시 협업과 각 팀원 PC 동기화 완료는 이번 자동 검사로 확인할 수 없다. 기존 UI 시연의 영상 이동은 HTML 재생 엔진에서 검증했다.
