# 오버레이 구조 · 피드백 32/35 재보완 수정 계획서 작성 기록

> 계획서 원본(유일한 진실): `C:\BAEframe\BAEFRAME_수정계획_32-35-재보완.md` (저장소 밖).
> 이 파일은 작성 기록일 뿐 계획서 사본이 아니다. 구현 커밋에 포함해도 된다.

## 요약

| 항목 | 내용 |
|------|------|
| 배경 | v1.5.0-beta 실사용 재보고 — 32 잔존(재생 중 UI 확대·재등장), 35 미흡(피드백·재선택·다중), 신규(드래그드롭 차단), 구조 지시(mpv 가림 문제 근절) |
| 작업 수 | 4개 — ①미러 무-diff 재주입 제거+playhead 필드 분리 ②드래그드롭 수복(document 가드+will-navigate 채널 라우팅) ③획 선택 v2(벡터 커밋·Shift/마퀴 다중·hover) ④드로잉/댓글 모드 하이브리드 엔진(코덱 게이트+engineSwap 경량 전환+설정 토글) |
| 작성 기준 | 2026-07-16, main HEAD `afbf40e`(v1.5.0-beta), 테스트 기준선 726 pass / 0 fail (18개 스위트) |
| 원인 확정 | ① 미러가 diff 없이 매 sync innerHTML 통째 재주입 → `.video-comment-range-bar.active` scaleY(1.3) 스냅샷·pulse 재시작 반복(댓글 다수 파일에서 발현) ② `#dropZone`이 로드 후 display:none + 호스트 네이티브 창이 OLE 드롭 가로챔 + will-navigate 가드 전무 ③ 커밋의 `_freezeStrokeRecords` 전면 동결 + hover 무처리 + 단일 floating ④ mpv 네이티브 창 위 DOM 불가(HTML5 엔진에서는 우회 전부 가드로 꺼짐 — 검증) |
| 검증 이력 | 2패스 독립 리뷰 + 델타 검증. 블로커 9건 검출·반영(epoch 캐시, 미러 테스트 2건, 드롭 채널 분기, additive 이중 차단, 홀드 프레임 소실, 하이브리드 전이 토큰/댓글 자기취소, 모드-off 경합, engineSwap 스킵 크래시, hover 死코드) + playhead diff 무력화 해소 |
| 실행 | 브랜치 `fix/오버레이구조-32-35`, 작업당 1커밋, PR → codex-review-loop → baeframe-release-deploy. **주의: npm run format 금지(prettier 미관리 저장소), 변경 파일에만 eslint --fix** |
