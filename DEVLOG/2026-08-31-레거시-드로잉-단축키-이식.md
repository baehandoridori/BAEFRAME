# 레거시 드로잉 단축키 16종 mpv 파일럿 이식

## 요약

| Phase | PR | 수정 파일 | 이유 | 우선순위 | 상태 |
|------|----|----------|------|---------|------|
| ① 프레임·키프레임 조작 7종 | #212 | `mpv-fabric-overlay-runtime.js`, `mpv-overlay-host.js`, `fabric-drawing-pilot-controller.js`, `app.js` | `2`/`3`/`Shift+2`/`Shift+3`/`4`/`Ctrl+Alt+C`·`V` 를 씬 집합 조작으로 옮긴다 | 높음 | ✅ 완료 |
| ② 레이어 데이터 모델 | #213 | `shared/drawing-layers.js`(신규), `review-data-manager.js` | 저장 스키마를 건드리지 않고 레이어를 담을 자리를 만든다 | 높음 | ✅ 완료 |
| ③ 레이어 추가·선택 + 타임라인 행 | #214 | `app.js`, `main.css`, `docs/drawing-keyframe-features.md` | `Shift+F1`/`Shift+X`/`Shift+C`. 메타데이터만으로 완결되는 것부터 | 높음 | ✅ 완료 |
| ④ 표시·잠금 (뷰 상태) | 이 문서 | `mpv-fabric-overlay-runtime.js`, `mpv-overlay-host.js`, `ipc-handlers.js`, `preload.js`, `fabric-drawing-pilot-controller.js`, `app.js` | `` ` ``/`Ctrl+2`. 오버레이가 숨기고 잠가야 의미가 있다 | 높음 | ✅ 완료 (v2.8.0-beta) |
| ⑤ 삭제·순서 (문서 변이) | 후속 | 위 + 구조 히스토리 | ``Shift+` ``/`Ctrl+Shift+X`·`C`. 오브젝트를 지우고 순서를 바꾼다 | 중간 | ⬜ 대기 |

## 배경 및 목적

레거시 드로잉 모드(캔버스 기반)에는 애니메이터가 쓰던 키프레임·레이어 단축키 16종이 있었다. mpv 파일럿(fabric 오버레이)으로 옮기면서 이 키들은 전부 **방화벽에서 차단**돼 있었다 — 누르면 아무 일도 일어나지 않는다. `docs/drawing-keyframe-features.md` 가 어느 키가 왜 막혀 있는지의 단일 참조다.

막아 둔 이유는 파일럿의 데이터 모델이 다르기 때문이다. `drawingsV3` 는 키프레임마다 오브젝트 목록만 있고 **레이어 개념이 없다.** 게다가 이 스키마는 불가침이라(레코드는 정확한 키 집합으로 3개 층에서 검증한다) 레코드에 레이어 필드를 넣을 수 없다.

그래서 이 라운드는 두 갈래로 나눴다:

- **씬 집합만 바꾸면 되는 것**(프레임 조작) → 런타임의 `targetFrame` 재키잉으로 해결(①)
- **레이어가 필요한 것** → `.bframe` **루트의 `drawingLayers`** 에 레이어 모델을 따로 두고, 오브젝트 id → 레이어 id 배정으로 그림과 잇는다(②③④⑤)

루트가 확장 가능한 것은 `shared/bframe-root-envelope.js`(2026-07-23) 덕분이다 — 앱이 모르는 최상위 필드를 원문 그대로 보존한다.

## Phase ④: 표시·잠금 (뷰 상태)

### 목표

`` ` `` 로 활성 레이어를 숨기고, `Ctrl+2` 로 잠근다. 타임라인 행의 눈·자물쇠 버튼도 같이 살린다.

### 왜 "뷰 상태" 인가

숨김·잠금은 **문서에 쓰면 안 된다.** 레코드 키 집합이 고정이라 `visible`/`locked` 를 넣을 자리가 없다. 대신:

- 레이어의 `visible`/`locked` 는 이미 `drawingLayers` 모델에 있다(②에서 만듦)
- 렌더러가 그 플래그 + 배정으로 **숨길 오브젝트 id 집합**·**잠글 id 집합**을 계산해 오버레이로 밀어 넣는다
- 오버레이는 그 집합을 들고 있다가 **씬을 그릴 때마다** 적용한다

집합을 들고 있어야 하는 이유: 프레임을 넘기거나 실행취소가 돌면 캔버스를 새로 만든다. 토글을 한 번 보내는 방식이면 그 순간 숨긴 획이 다음 프레임에서 되살아난다.

### 수정 파일

| 파일 | 내용 |
|---|---|
| `preload/preload.js` | `mpvUpdateOverlayDrawingLayerView` 추가(기존 `...DrawingTool`/`Brush`/`Frame` 과 같은 형태) |
| `main/mpv-overlay-host.js` | IPC 핸들러 + 런타임 호출. 요청 검증(배열 길이·문자열 id) |
| `renderer/scripts/modules/mpv-fabric-overlay-runtime.js` | `layerViewState` 보관. `makeFabricPath` 에서 `visible`, `refreshSelectionInteractionPolicy` 에서 `selectable`/`evented` 적용 |
| `renderer/scripts/modules/fabric-drawing-pilot-controller.js` | `sendLayerView(state)` — 세션이 살아날 때 재전송 |
| `renderer/scripts/app.js` | 단축키 2종, id 집합 계산, 레이어 상태 변경 시 push, 숨김·잠금 레이어에 그리기 차단 토스트 |
| `renderer/styles/main.css` | 파일럿 행의 눈·자물쇠 버튼 숨김 해제 |

### 예상 리스크

| 리스크 | 심각도 | 완화 방안 |
|--------|--------|----------|
| 숨긴 오브젝트가 저장 스냅샷에서 빠진다 | **높음** | 레코드는 `id/type/pathData/sourcePoints/style/transform` 만 만든다. fabric 의 `visible` 은 읽지 않는다 — 소스 테스트로 고정 |
| 캔버스 재구성 경로 누락 | 높음 | `makeFabricPath` 한 곳에서 적용해 모든 재구성 경로가 지나가게 한다 |
| 잠근 레이어에 계속 그려진다 | 중간 | 렌더러가 활성 레이어의 잠금·숨김을 먼저 보고 토스트로 막는다(레거시와 같은 문구) |
| 세션 재시작 후 집합 유실 | 중간 | `active` 전이에서 재전송 |

### 테스트 방법

- `node --test scripts/tests/fabric-drawing-pilot-source.test.js` — 단축키·집합 계산·차단
- `node --test scripts/tests/mpv-fabric-overlay-runtime.test.js` — 렌더 적용·저장 스냅샷 불변
- 전체 스위트로 회귀 확인. 각 수정은 되돌려 실패하는지 확인한다

### 구현 결과 (v2.8.0-beta)

배선은 계획대로 한 방향이다: **렌더러가 계산하고 오버레이는 적용만 한다.**

```
레이어 모델(drawingLayers)  →  id 집합 계산  →  IPC  →  오버레이
 visible / locked 플래그        fabricPilotLayerViewSets()      hiddenObjectIds
 + 오브젝트 배정                                                lockedObjectIds
                                                                activeLayerDrawable
```

- 오버레이는 레이어를 모른다. `makeFabricPath` 에서 `visible` 을, `refreshSelectionInteractionPolicy` 에서 `selectable`·`evented` 를 집합으로 판정한다. 두 곳 모두를 거치므로 **캔버스를 다시 만들어도**(프레임 전환·실행취소) 유지된다.
- 활성 레이어가 숨겨졌거나 잠겼으면 `onPointerDown` 이 새 획을 받지 않는다. 선택 도구는 통과시킨다 — 다른 레이어의 획은 여전히 다뤄야 한다.
- 저장 문서는 손대지 않는다. 런타임 테스트가 숨김 적용 전후의 `exportDrawingVideo` 스냅샷이 **완전히 같음**을 단언한다.
- 호스트는 리비전·토큰·id 배열 형식·`activeLayerDrawable` 의 boolean 여부까지 검사한다. 거절은 리비전을 전진시키지 않는다.

검증: 25개 스위트 2,349 pass / 0 fail. 각 수정을 되돌려 실패를 확인했다(런타임 4건, 호스트 1건, 렌더러 3건).

### 남은 것 — Phase ⑤

``Shift+` ``(레이어 삭제)와 `Ctrl+Shift+X`·`C`(순서 이동)는 **문서를 바꾼다.** 삭제는 그 레이어의 오브젝트를 모든 키프레임에서 지우고 실행취소로 되살려야 하고, 순서는 씬의 오브젝트 순서를 레이어 순서에 맞춰 다시 배열해야 한다(새 획이 들어올 때마다 유지되어야 하므로 일회성 정렬로는 부족하다). 대조표에는 계속 **차단**으로 적혀 있다.
