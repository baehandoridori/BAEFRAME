# 드로잉 엔진 안정화 수정계획 작성 기록

> 이 문서는 **작성 기록**이다. 계획서 본문은 `C:\BAEframe\BAEFRAME_수정계획_드로잉엔진-안정화.md` (저장소 밖, 단일 원본) — 내용을 이 문서에 복사하지 않는다.

## 개요

| 항목 | 값 |
|------|-----|
| 작성일 | 2026-08-14 |
| 기준 스냅샷 | `main` HEAD `f1bd96e` (v2.0.3-beta, PR #173) |
| 피드백 출처 | 사용자(배한솔) 구두 보고 — 구글독스 아님 |
| 범위 | 드로잉 엔진 기본 사용 문제 3건: ① B 토글 즉시성, ② Ctrl+Z 전역 undo(하이라이트), ③ 드로잉 원격 표시 |
| 계획서 경로 | `C:\BAEframe\BAEFRAME_수정계획_드로잉엔진-안정화.md` |
| 작업 수 | 6개 (undo 잼 해소 / 댓글 undo 시점 / fabric→전역 undo 폴백 / B 토글 큐잉 / drawingsV3 동기화 신설 / mpv 입력 인자) |
| 테스트 기준선 | `test:*` 25종 전부 PASS (계획서 헤더에 기록) |
| 실행 상태 | ⬜ 미착수 — 실행 에이전트 핸드오프 대기 |

## 근본 원인 요약 (코드로 확정)

1. **B 토글**: 배포 빌드는 fabric-v3-stable 마커로 항상 Fabric 파일럿 경로 — `toggle()`이 'recovering'·persistence 소스 갱신 중 B를 침묵 폐기(`fabric-drawing-pilot-controller.js:1506`). 레거시 폴백은 하이브리드 스왑 구간 준비중 표시 누락(`app.js:10004`).
2. **Ctrl+Z**: (a) 하이라이트 undo/redo 콜백이 getter 전용 `colorInfo`에 대입 → strict mode TypeError → `globalUndo`의 실패 되밀기로 영구 잼(`app.js:4232·4598·1126`), (b) Fabric 파일럿이 렌더러(`routeKeydown`)·main(`before-input-event`) 양쪽에서 Ctrl+Z를 소비하고 히스토리 비어도 전역 폴백 없음, (c) 댓글 pushUndo가 save await 뒤(`app.js:2170`).
3. **원격 미표시**: Fabric 드로잉(`drawingsV3`)은 Liveblocks 브로드캐스트 미구현 + 파일 동기화는 `isConnected` 시 전면 차단(`app.js:17098`) → 전파 채널 0개. 늦은 합류자 seed 부재.

## 검증 이력

- **1차 — 근본 원인 탐색**: 4방향 병렬 탐색(B토글·undo·동기화·설계문서) 후 원인 주장 16건 적대적 교차검증 → 13건 유지, 3건 반박(레거시 경로를 배포 경로로 오인한 원인 2건 강등, freeze 드리프트 "조용한 실패" 주장은 기존 복구 계층 확인으로 폐기).
- **2차 — 테스트 영향 전수 조사**: 2방향. 산출: mpv-overlay-host.test.js deepEqual 5건 회피 구현(조건부 spread), fabric-drawing-pilot-shortcuts.test.js 행위 테스트 1건 정확 갱신, `createMpvLaunchArgs` 함수명 교정, 금지어·완전일치 단언 함정 반영.
- **3차 — 계획서 검증**: 사실 검증 2방향(모든 before 블록 글자 단위 대조·정규식 시뮬레이션) + 구현 시뮬레이션 1방향. **블로커 4건** 발견·반영:
  1. 작업 3(y) 신규 단언 정규식이 (a) 조건부 spread 구현과 모순 → 정규식 교체.
  2. Broadcast 적용 후 Drive 지연 창에서 구버전 디스크가 재설치·재브로드캐스트되는 stale 에코/저장 잼 → **(a-0) 스테일 디스크 지문 가드 신설**.
  3. 늦은 합류자가 구버전 로컬 상태를 방에 seed하는 경합 → (f) 자기-seed 10초 억제.
  4. 영상 전환 직전 수신된 루트가 새 영상에 오염 적용되는 경합 → `_enqueueApply` started/경로 가드.
- **4차 — 수정분 재검증**: 델타 패스 — 블로커 1건 추가(drawingsV3 지문이 전체 JSON이라 와이어 탑재 시 Liveblocks 1MB 한도 초과) → `hashFingerprint` 와이어 해시 도입, 스테일 마킹을 모든 디스크 상태 교체 지점으로 일반화.
- **5차 — 구현 단계 발견 (2026-08-15)**: 실행 에이전트(Codex)가 작업 4 진행 중 경합 재현 테스트로 계획서 결함 1건 발견 — hydration await 창의 B 취소가 차단되지 않아 취소 후 드로잉 모드가 재점화. 계획서 (a-4)로 명문화(hydration 후 `isCurrentInputRequest` 재검사 + 회귀 테스트)해 승인·구현 완료(`eeb8bb1`). 실행 에이전트의 "before 불일치·계획 밖 수정 시 중단·보고" 규율이 작동한 사례.
- **6차 — 작업 5 사전 감사 (2026-08-15)**: Codex가 착수 전 감사로 블로커 2건 추가 발견, 코드 검증 후 계획서 반영 — ① `reloadDrawingsV3FromDisk` 파일 전환 경합(읽기 await 중 영상 전환 시 이전 파일 drawingsV3가 새 컨텍스트에 설치; owner 캡처→await 후 재검증→reconcile 명시 전달로 수정, 기능 테스트 계약 추가) ② 협업 방 전환 시 `_previousOthersCount` 잔존(`LiveblocksManager.stop()`이 collaboratorsChanged를 발생시키지 않음; 새 세션 시작 시 기준값·시작 시각 명시 초기화로 수정, 소스 단언 추가). 진행 상황: 작업 1~4 커밋 완료(`3c3f414`·`1e5d5cc`·`f3e26e8`·`eeb8bb1`), 테스트 1,866/1,866 통과, 작업 5·6 진행 예정. 추가로 Codex 2차 사전 감사(08-15)에서 계획서 문구 결함 2건 정정 — ① owner 인자 설명이 갱신된 after 코드와 모순(applyExternal=기본 캡처 충분 / reload=명시 전달 필수로 구분 명기) ② 경합 기능 테스트 계약이 존재하지 않는 provider mock을 참조(실제 `createFabricStore()` store의 `importRootValue` spy + `exportRootValue()` deepEqual 사양으로 고정).
- **7차 — 작업 5 품질 검토 발견 (2026-08-15)**: Codex가 작업 5 구현본(커밋 `bd74c3c`, 1,870/1,870 통과) 품질 검토에서 경합 결함 1건 확정 — FabricDrawingSync `_applyQueue`가 stop→start 경계를 넘어 생존해, 같은 `.bframe` 재접속(A→B→A) 시 이전 세션의 대기 적용이 `_started`·파일 경로 가드를 통과해 새 세션에 설치됨. 계획서에 `_sessionGeneration` fence(stop 시 증가, 큐 항목이 캡처·비교)로 반영, in-flight 적용은 rdm owner epoch이 이미 담당함을 명기(reconcile 쪽 세션 가드 추가 금지 결정), 기능 테스트 계약(A1 보류→A2 큐→stop/start·A→B→A→A1 resolve→A2 적용 0건·A3 정상 적용) 추가. 작업 5 커밋은 amend로 1커밋 유지 예정.

## 후속 체인

1. 실행 에이전트 구현 (핸드오프 프롬프트로 전달) → 브랜치 `fix/드로잉엔진-안정화` → PR
2. `codex-review-loop` 스킬로 PR 리뷰 루프
3. 머지 후 `baeframe-release-deploy` 스킬로 빌드·배포·해시 검증
4. 배포 타임스탬프를 프로젝트 메모리에 기록 (다음 피드백 상태 대조 기준)
