# Certificates Folder

Place your team distribution certificate (`.cer`) here.

Recommended file name:
- `StudioJBBJ.BAEFRAME.Integration.cer`

This certificate should be the public certificate used to sign the integration package.
Do not commit private keys (`.pfx`) into the repository.
