# BAEFRAME 공통 작업 규칙

Codex와 Claude Code가 함께 사용하는 저장소 지침이다. 사용자의 현재 요청과 명시적으로 지정한 사양을 우선한다.

## 작업 시작

- 사용자는 비개발자다. 무엇이 달라지는지 먼저 쉬운 한국어로 설명하고, 기술 설명은 필요한 만큼 덧붙인다.
- `git status --short`, 현재 브랜치와 `package.json`을 확인한다. 다른 작업의 수정·미추적 파일을 보존한다.
- 기존 작업과 충돌하면 별도 worktree에서 진행한다. 강제 초기화, 무관한 파일 삭제, `git add -A`, 저장소 전체 포맷을 피한다.
- 새 브랜치는 Codex에서 `codex/`, Claude Code에서 `claude/`를 기본으로 한다. 사용자가 지정한 이름은 그대로 따른다.
- 지정 계획서가 있으면 먼저 읽고 범위를 지킨다. 사용자가 before 블록 일치를 요구했는데 다르면 임의로 적용하지 않고 해당 부분을 보고한다.

## 작업별 참고 문서

| 작업 | 먼저 읽을 문서 |
|---|---|
| 개발 환경·구조·테스트 | [개발 가이드](docs/development-guide.md) |
| 드로잉·키프레임·레이어·실행취소 | [드로잉 작업 규칙](docs/drawing-work-guide.md), [현재 기능표](docs/drawing-keyframe-features.md) |
| PR·리뷰·머지·빌드·배포 | [릴리스 가이드](docs/release-guide.md) |
| 협업·파일 저장 | [협업 구조](docs/collaboration.md), [파일 명세](docs/bframe-schema.md)와 실제 검증 코드 |

## 변경과 검증

- `.bframe` 하위 호환성을 유지한다. `drawingsV3` 레코드에 임의 필드를 추가하거나 레거시 `drawings`에 이중 저장하지 않는다.
- `main/`·`preload/` 수정은 IPC 입력 검증과 보안 경계를 함께 확인한다. 바이너리 교체는 작업 범위에 명시된 경우에만 한다.
- 오버레이 런타임을 수정하면 `npm run bundle:mpv-fabric-overlay`로 생성 번들을 갱신하고 같은 변경에 포함한다. 생성 파일만 직접 고치지 않는다.
- 변경 영역에 맞는 테스트를 실행한다. 실패를 고친 뒤 새 근거 없이 같은 검증을 반복하지 않는다. 문서만 바꾸면 링크·명령·내용 일치 여부를 검사한다.
- 자동 테스트, 빌드, 실제 앱 조작을 구분한다. 테스트는 종료 코드와 pass/fail/cancelled를 기록하고, 수행하지 않은 검증은 명시한다.
- 버전과 기능 상태는 현재 코드·설정·검증 결과로 확인한다. 과거 DEVLOG의 완료 수치를 이번 실행 결과로 보고하지 않는다.

## 작업 범위와 마무리

- 이미 승인된 작업은 재확인 없이 진행한다. PR 생성 승인은 머지·배포·Slack 게시 승인으로 확대하지 않는다.
- PR 생성 요청이면 제목·본문을 준비한 뒤 바로 생성한다. 초안만 요청했으면 생성하지 않는다.
- 커밋은 무엇을 어떻게 왜 바꿨는지 간략한 한국어로 작성한다. PR 형식과 릴리스 완료 기준은 릴리스 가이드를 따른다.
- 복잡한 작업은 DEVLOG에 계획·진행·검증 근거를 남긴다. 현재 기능표에는 최신 상태만, 과거 시행착오는 날짜가 있는 기록에 보존한다.
- 완료 보고는 사용자에게 달라지는 점, 검증 결과, 미확인 사항과 남은 작업을 포함한다.
